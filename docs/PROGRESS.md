# Журнал работы / с чего продолжать

> Живой лог по сессиям. Самое свежее — сверху. Подробности фич — в `SPEC.md`.

---

## 2026-06-15 (день-18b) — мелкие правки Здоров'я
- Убрал кнопку «Підтягнути всю історію (разово)» из Здоров'я.
- Убрал показатель «Кроки» из снимка (кольцо носят не всегда — вводило в заблуждение).
- `getLatestSnapshot` теперь берёт последний день с **готовністю/сном** (а не просто свежайшую дату) — показывает реальное «утреннее» чтение, не маскируется днём без кольца. Клиент, деплой не нужен.

---

## 2026-06-15 (день-18) — планёрка по экранам + бэклог

### ✅ Зафиксировано
- Завёл **`docs/BACKLOG.md`** — собрал все идеи/решения с обсуждения: IA-вкладки (Головна/Програми/Коуч/Аналітика/Профіль + условное Здоров'я), ред./удаление программ, удаление тренировки с варнингом, ІІ-коуч (чат + голосовое STT, без TTS), правка даты цикла, режим «з кентом», пол при онбординге→цикл, чистка дисциплін, кастомные эспандеры без RGC, цикл пентеста/архитектуры, локально-vs-интернет, токеномика.
- Черновой порядок работ — в конце BACKLOG.

### 🔜 Следующее (на согласовании, см. BACKLOG §Порядок)
1. Данные-safety (ред./удаление программ, удаление тренировки с подтверждением, правка даты цикла).
2. Профіль/онбординг (пол→цикл, дисципліни, фикс «Мої вправи», кастомный эспандер).
3. ІІ-коуч.
4. IA-переезд вкладок.
5. Архитектура + пентест.

---

## 2026-06-15 (день-17) — Аналитика: рекорды-дерево + доводка

### ✅ Сделано
- **Формула 1ПМ → О'Коннор** (`вес×(1+0.025·повт)`) — честнее, ниже Эпли.
- **Плитка «Час тренувань»** (сумма длительностей), значения на столбцах тоннажа + подпись «кожен стовпчик — тренування» (раньше все подписи `15.6` от тестовых тренировок в один день).
- **Рекорди — дерево «кластер → вправа → топ-5»** (выбран формат «Топ-5 по 1ПМ + кластери»):
  - Группировка по кластеру (Верх/Низ/Все тіло/Кор/Інше), разворачивается.
  - Внутри — упражнения с хедлайном (лучший ≈1ПМ или лучшая длительность), разворачиваются.
  - Топ-5 подходов, ранжир по **оценке 1ПМ** (О'Коннор) → `110×6` честно выше `92.5×10`; дедуп одинаковых вес×повт (ранняя дата). **Даты** у каждого.
  - **Временные рекорды** (планки/удержания): отдельный ранжир по **длительности** (`3:04`, с весом если был).
- Считается на клиенте из истории (последние 30 тренувань). Деплой не нужен.
- Проверка: `tsc` ✅, web ✅.

### ✅ Дополнено — PR по всей истории
- Аналитика больше **не капнута на 30 тренировок**. Новый лёгкий плоский запрос `getLoggedSets` (`db/analytics.ts`) тянет все отмеченные подходы (только нужные поля, с пагинацией по 1000, RLS по юзеру), `analyze` переписан под плоские строки. Тяжёлую вложенную `listWorkouts` для аналитики больше не используем. SQL-RPC оставлен как следующий тир — только если объёмы станут большими.

### 🔜 Дальше
- **Recovery-корреляции** (readiness/сон/HRV/цикл ↔ тренировки) и **прогресс хвата** — следующие срезы аналитики.
- **IA/«переиграть экраны и фичи»** + ревизия по ТЗ — отдельная сессия планирования (что кому показывать, порядок табов, скрытие лишнего).

---

## 2026-06-15 (день-16) — Аналитика: тренировочное ядро (v1)

### ✅ Сделано
- **Экран «Аналітика» ожил** (`src/app/(tabs)/analytics.tsx`) — считаем на клиенте из истории тренировок (`listWorkouts`, последние 30). Без новых зависимостей.
  - Плитки: кол-во тренировок, общий тоннаж (в активной единице), всего подходов.
  - **Тренд тоннажа** — бары по последним 12 тренировкам (на `View`, без SVG).
  - **Рекорди** — по упражнению лучшая **оцінка 1ПМ** (Эпли: вес×(1+повт/30)), с подписью лучшего веса×повторов; сортировка по 1ПМ.
  - Учитываются только отмеченные (`logged_at`) силовые подходы; удержания (`duration_sec`) в тоннаж/1ПМ не идут.
- Проверка: `tsc` ✅, web ✅. Деплой не нужен (чистый клиент).

### ⏭️ Следующее (аналитика, фаза 2)
- **Recovery-корреляции:** наложить readiness/сон/HRV/фазу цикла на лёгкость/успешность тренировок «по календарю» (`getSnapshotsRange` + джойн по дате; нужна история OURA).
- **Прогресс хвата:** динамика по эспандеру/RGC/установке (TNS/блок) и динамометру из `sets.meta`.
- По желанию: per-exercise тренд (тап по рекорду → график 1ПМ во времени), SVG-линии (`expo install react-native-svg`), вынос тяжёлых агрегатов в SQL-RPC если разрастётся.

---

## 2026-06-15 (день-15) — фикс safe-area в нижних листах + с чего продолжить

### ✅ Сделано
- **Фикс «кривых» экранов.** Нижние листы (bottom sheets) прижимались к краю и кнопки лезли под системную навигацию Android (edge-to-edge). Добавил нижний `insets.bottom` отступ: «Мої еспандери» (`EditGripper`), «Мої вправи» (`EditExercise`), выбор эспандера (`GripPicker`). Кнопки «Зберегти/Видалити/Готово» больше не клипаются.
- Проверка: `tsc` ✅.

### ▶️ С чего продолжить завтра
- **Деплой хвостов** (если ещё не катал): `npx supabase functions deploy oura-sync` (пагинация/полная история). Миграции 210000–250000 — `db push`.
- **Следующая большая фича — АНАЛИТИКА** (фаза 2). Почва готова: дневные снимки OURA копятся (`getSnapshotsRange`), тренировки/подходы/RPE/тоннаж есть, цикл считается из `cycle_periods`, grip-подходы пишут `sets.meta` (эспандер+установка). Что строить:
  1. SQL-вьюхи/RPC: тоннаж/объём по дням, оценка 1ПМ, PR по упражнениям, прогресс по эспандеру (по RGC/установке).
  2. Экран «Аналітика»: тренды + наложение readiness/сна/HRV/фазы цикла на лёгкость/успешность тренировок («по календарю»).
  3. Графики (выбрать лёгкую RN-либу или рисовать SVG).

---

## 2026-06-15 (день-14) — OURA: богатый дневной снимок + сохранение каждого дня

### ✅ Сделано
- **Чиним накопление истории.** Раньше `oura-sync` тянул 2 дня и сохранял **только последний** (перезапись) — ряд не копился. Теперь тянет диапазон (по умолчанию **30 дней**, можно `body.days` до 180) и **апсертит КАЖДЫЙ день** (`onConflict user_id,date`) → накапливается time-series для аналитики.
- **Тянем всё, что отдаёт OURA v2:** `daily_readiness`, `daily_sleep`, `sleep` (детальный), `daily_activity`, `daily_spo2`, `daily_stress`, `daily_resilience`, `daily_cardiovascular_age`, `vO2_max`. Отсутствующие/недоступные эндпоинты тихо пропускаются.
- **Богатый снимок дня** (миграция `20260615250000`): колонки — recovery (readiness, temp, temp_trend), sleep (hrv, rhr, avg_hr, respiratory_rate, total/deep/rem/light/in-bed мин, efficiency, latency, restless, bedtime_start/end), activity (score, steps, active/total ккал, дистанция, MET, времена активности), spo2_avg, breathing_disturbance_idx, stress_high/recovery_high мин + summary, resilience_level, vascular_age, vo2_max. Плюс `*_contributors` (jsonb) и полный `raw` — ничего не теряем.
- Клиент: `oura.ts` — расширен тип `HealthSnapshot`, добавлен `getSnapshotsRange(userId, from)` для будущей аналитики «по календарю». Экран Здоров'я работает как прежде (читает `raw`).
- **Хранение вечное + полная история.** Данные не удаляются (только upsert). Добавлена **пагинация по `next_token`** (иначе OURA режет >250 строк) и лимит диапазона поднят до ~2000 дней. В Здоров'ї — кнопка **«Підтягнути всю історію (разово)»** (`syncOura(1825)` ≈ 5 лет) для разовой загрузки прошлого.
- Проверка: `tsc` ✅, JSON ✅.

### 🚧 TODO (деплой) — ты
- [ ] `npx supabase db push` (миграция `20260615250000`)
- [ ] `npx supabase functions deploy oura-sync` (вкл. пагинацию — пере-деплой)
- [ ] В приложке Здоров'я → «Оновити» (30 дней) или один раз «Підтягнути всю історію». Цикл (`cycle_day/phase`) аналитика выведет из `cycle_periods` при джойне по дате — отдельной записи в снимок не требует.

### ⏭️ Следующее
- **Аналитика** (фаза 2): связки readiness/сон/HRV/цикл ↔ лёгкость/успешность тренировок, тренды, тоннаж/1ПМ/PR, прогресс по эспандеру.

---

## 2026-06-15 (день-13) — ревизия каталога: база + словники дисциплін + сила хвата

### ✅ Сделано
- **Детали саммари — по кластерам.** Завершённая тренировка в «Деталі» группирует упражнения как в самой тренировке (EMOM/E2MOM/суперсети — одним блоком с акцентным заголовком, в исходном порядке), а не вразброс. `summary/[id].tsx`.
- **Архитектура «база + словники дисциплін».** Новый флаг `exercises.is_base` (база видна всем) + `profile.disciplines text[]` (включённые специфические словники). Пикер: просмотр = база + включённые дисциплины + свои; **поиск пробивает всё**; при выборе упражнения из ещё не включённой дисциплины её словник **тихо включается** (`disciplineToEnable`/`enableDiscipline`). В Акаунті — мультивыбор «Мої дисципліни».
- **Ревизия каталога.** Миграция `20260615230000`: размечен `is_base` (вся «general» + базовые Pull-up/Push-up/Sit-up/Hanging Leg Raise), удалена мусорная «Імітація руху» (безопасно, без падения на FK).
- **Новый словник «Сила хвата» (grip).** Категория `grip` (+CHECK), миграция `20260615220000`: `sets.meta`/`program_sets.meta` (jsonb под специфику), `exercises.log_kind` ('gripper'), таблица `grippers` (имя + RGC + ед.). Упражнения: Стиснення еспандера (повторы), Статичне утримання еспандера (⏱), Динамометр (кг). Plate Pinch → grip, но базовый. В тренировке для эспандера в `SetRow` — выбор эспандера + вид установки (TNS/з карти/блок 38/блок 20/дип-сет), пишется в `sets.meta`.
- **Армрестлінг-словник расширен** (луч, кисть широкою/роллінгом, імітація верхом/в крюк, відведення, натяжка через відведення, натяжка за столом ⏱).
- **«Мої еспандери»** (`grippers/index.tsx`, вход из Акаунта): добавить/править/удалить эспандер (имя + RGC + kg/lb).
- **Глобальный каталог эспандеров** (миграция `20260615240000`): спарсен чарт Cannon Powerworks — 103 эспандера, 11 брендов (CoC, Tetting, Standard, Grip Genie, RB, GHP, Mash Monster, Heavy Grips, Hybrid, Left-Turn, CPW Hybrid), RGC=AVERAGE в lb. `grippers` стала глобальной+личной (RLS: глобальные видят все, пишут только свои). В выборе эспандера — поиск + секции: **личные сверху**, дальше по брендам, RGC показывается в lb и ≈кг.
- Проверка: `tsc` ✅, JSON-локалі ✅.

### 🚧 TODO (деплой) — клиент и БД меняются вместе
- [ ] `npx supabase db push` (миграции `20260615210000`, `20260615220000`, `20260615230000`, `20260615240000` — если ранние ещё не катал).
- [ ] `npx expo start -c` + Reload. **Важно:** пикер фильтрует по `is_base`/дисциплинам — до `db push` каталог в просмотре будет пустой (нужны новые колонки). После — включи свои дисциплины в Акаунті (армрестлінг, сила хвата), добавь эспандеры.

### ⏭️ Следующее
- Возможна доводка grip-аналитики (прогресс по эспандеру/RGC).
- **Аналитика** (фаза 2).

---

## 2026-06-15 (день-12) — планки/удержания в секундах

### ✅ Сделано
- **Временной тип подхода.** У упражнения появилась метрика `metric` (`reps` | `time`). Для временных упражнений в тренировке поле «повт.» заменяется на «сек» (вес остаётся опциональным — для утримань з вагою). Логика надёжна к импорту: поле показывается как секундное, если у упражнения `metric='time'` **или** у самого подхода уже задана `duration_sec`.
- **Схема.** Миграция `20260615210000_time_based_sets.sql`: `exercises.metric` (default 'reps' + check), `sets.duration_sec`, `program_sets.target_duration_sec`. Явные удержания глобального каталога помечены `time` по точному имени: Dead Hang, Handstand Hold, Plank, Side Plank, Hollow Hold, L-sit.
- **Саммари.** Временные подходы показываются как `60с` (с весом — `8 кг · 60с`), добавлена плитка **«Утримання»** (сумма секунд удержаний за тренировку).
- **Программы.** `program_sets.target_duration_sec` переносится в подходы при старте тренировки; деталь программы показывает секунды. Промпт `program-import` теперь кладёт длительность в `duration_sec` (а не в notes); «20/20 сек на руку» → 20с + «per side» в notes.
- **Менеджер «Мої вправи».** Добавлен переключатель метрики (Повтори / Секунди) — так можно вручную пометить любое своё упражнение временным.
- Проверка: `tsc` ✅, JSON-локалі ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615210000`)
- [ ] `npx supabase functions deploy program-import` (длительность в `duration_sec`)
- [ ] `npx expo start -c` + Reload. Для удержаний из каталога секунды появятся сразу; для импортированных программ — **переимпорт** (чтобы планки получили duration).

### ⏭️ Следующее
- **Ревизия 82 глобальных** (выгрузка на правку — заодно дочистить метрики удержаний).
- **Аналитика** (фаза 2).

---

## 2026-06-15 (ночь-11) — скорость старта, сворачивание, префил, чистые имена

### ✅ Сделано
- **Старт тренировки в разы быстрее.** Было: десятки последовательных запросов (упражнение + каждый подход по очереди). Стало: пакетная вставка — 1 запрос на все упражнения + 1 на все подходы (`startWorkoutFromProgram` переписан). 
- **Красивый экран загрузки** вместо «кружочка»: оверлей «Завантажуємо вашу програму — майже готово…» (`program/[id]`).
- **Сворачивание по умолчанию:** при старте развёрнут только **первый** блок/упражнение, остальные свёрнуты; тап разворачивает. Унифицированное состояние `collapsed` (упражнения+кластеры), завершение — сворачивает.
- **Понятный префил:** под полями подписи **«кг» / «повт.»** (раньше «5» и «6» без контекста).
- **Чистые имена + имя из программы в тренировке.** Миграция `20260615200000`: `workout_exercises.display_name` (имя «как в программе»); тренировка показывает его, не зависит от того, на что сматчилось. Промпт импорта: `name` теперь **чистое** (без веса/повторов: «млинів на руку з гирею 8 кг» → «Млини на руку»), односторонние — явно «… однією рукою».

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615200000`)
- [ ] `npx supabase functions deploy program-import` (чистые имена + per-side)
- [ ] `npx expo start -c` + Reload. **Переимпорт** программы — чтобы имена стали чистыми и матчинг применился (см. ночь-8 гард).

### ⏭️ Следующее
- **Планки/удержания — в секундах, а не повторах.** Нужен «временной» тип подхода (метрика reps vs sec). Сделаю отдельным заходом.
- **Ревизия 82 глобальных** (выгрузка на правку).

---

## 2026-06-15 (ночь-10) — EMOM/E2MOM раунд-за-раундом (C)

### ✅ Сделано
- **Правильный расчёт раундов.** `blockRounds`: EMOM/E2MOM = `duration ÷ interval ÷ кол-во упражнений` (EMOM16, 4 упр., 60с → 4 раунда; E2MOM16, интервал 120с → 4). «X кол» — как было. Раунды разворачиваются в подходы при старте тренировки.
- **Кластер в тренировке — по раундам.** Миграция `20260615190000`: `workout_exercises.block_type/block_interval_sec`. Рендер кластера переписан: «Раунд 1» → упр1, упр2, упр3, упр4 (r-й подход каждого), затем «Раунд 2» … Кнопки на уровне блока: **«Додати раунд»** (по подходу каждому упражнению), **«Завершити блок»/«Відновити блок»**. Имя упражнения вынесено в шапку строки (`SetRow headerLabel`).
- **Минутный таймер EMOM/E2MOM** «как факт» (`EmomTimer`): старт/стоп, показывает текущий интервал и сколько секунд осталось. Не принуждает.
- Одиночные упражнения — прежний экран-флоу. Отметка ✓/отдых работают и в раундах.
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615190000`)
- [ ] `npx expo start -c` + Reload. **Стартануть НОВУЮ тренировку** из EMOM-программы (раунды считаются при старте, переимпорт не нужен).

### 🔜 Дальше (согласовано)
- **Ревизия 82 глобальных упражнений** — выкинуть мусор, поправить имена/категории, добавить недостающее (мельница и пр.). Курируем сид (RLS не пускает править глобальные из приложения).

---

## 2026-06-15 (ночь-9) — редактор упражнений (A)

### ✅ Сделано (всё клиентское, деплой не нужен — только Reload)
- **Экран «Мої вправи»** (`src/app/exercises/index.tsx`, вход из Акаунта). Список своих кастомных упражнений; тап → лист редактирования: **переименовать (uk/en), задать кластер + категорию, удалить**. Удаление ловит FK (если используется в тренировках/программах → алерт «не можна видалити»).
- Data-слой: `listMyExercises`/`updateExercise`/`deleteExercise` + `CATEGORY_ORDER`/`ExerciseEdit` в `db/exercises.ts` (RLS уже пускает владельца на update/delete).
- Закрывает «адекватно заредачить»: мусорные имена/категории (напр. кастомную «імітація руху» → перейменувати в «імітація боротьби верхи…», задати категорію) можно править.
- Проверка: `tsc` ✅, web ✅.

### 🔜 Дальше (согласовано: A → C)
- **C. EMOM/E2MOM раунд-за-раундом**: правильный расчёт раундов (duration ÷ interval ÷ кол-во упражнений), отрисовка кластера по раундам (упр1-п1, упр2-п1…), минутный таймер EMOM как факт.
- Напоминание про качество: матчинг-гард + переимпорт старых программ (деплой `program-import` из ночь-8 — если ещё не сделан).

---

## 2026-06-15 (ночь-8) — фикс матчинга + кластеры в тренировке

### ✅ Сделано
- **Матчинг больше не «лепит» левые упражнения.** `program-import` доверял `catalog_index` от ИИ вслепую (млин з гирею → турецький підйом, розводи резинки → тяга до обличчя). Добавил серверную проверку схожести имён (`namesResemble`): индекс принимается, только если имена реально пересекаются токенами; иначе → кастомное упражнение с верным именем. **Нужно переимпортировать старые программы.**
- **Кластеры в тренировке.** Миграция `20260615180000`: `workout_exercises.block_key/label/rounds` (снимок блока программы). `startWorkoutFromProgram` переносит блок на упражнения и **разворачивает круги в подходы** (3 кола × 1 → 3 подхода). В тренировке упражнения одного круга сгруппированы под шапкой блока («3 КОЛА · 3×»), весь кластер **сворачивается/разворачивается** целиком (показывает N/M ✓). Одиночные упражнения — как раньше.
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615180000`)
- [ ] `npx supabase functions deploy program-import` (проверка схожести имён)
- [ ] `npx expo start -c` + Reload. **Переимпортировать Машину программу** (старая со «левыми» матчами).

### 🔭 На будущее
- Полноценный логгинг кругов «раунд за раундом» (сейчас круги развёрнуты в подходы по упражнению) — если понадобится.
- Аналитика (тоннаж/1ПМ/PR + графики, «по календарю» с readiness/сном/циклом).

---

## 2026-06-15 (ночь-7) — цикл (C) + фикс кнопки

### ✅ Сделано
- **Кнопка «Почати тренування»** в детали программы поднята над системной навигацией Samsung (safe-area `insets.bottom`).
- **Цикл (C).** Опция профиля `profile.track_cycle` (миграция `20260615170000`), переключатель в Акаунті. Когда включён — во вкладке Здоров'я карточка цикла: кнопка **«Позначити день 1»** (= начало менструации, сегодня), дальше **день и фаза считаются автоматически** до следующей отметки. Фазы: менструальна (1–5), фолікулярна (6–13), овуляція (14–16), лютеїнова (17+). `src/lib/db/cycle.ts` (`logPeriodStart`/`getCycleStatus`/`cyclePhase` + `get/setTrackCycle`), пишем в существующую `cycle_periods`.
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615170000` — `profile.track_cycle`)
- [ ] `npx expo start -c` + Reload. В Акаунті Маша включает «Відстежувати цикл», в Здоров'ї тапает «Позначити день 1».

### 🔜 Дальше
- Весь фидбек закрыт. Крупное следующее: **Аналитика** (тоннаж/объём/1ПМ/PR + графики, «по календарю» с readiness/сном/фазой цикла).

---

## 2026-06-15 (ночь-6) — OURA: все показатели + расшифровка (B)

### ✅ Сделано
- **Здоров'я: сетка всех показателей.** Readiness, Sleep, HRV, Пульс спокою, Температура, Дихання, Тривалість сну, Ефективність сну, Кроки. Показываются только заполненные.
- **Тап по показателю → нижний лист с расшифровкой**: «Що це означає» + «Орієнтири» (референсные диапазоны) на uk/en. Локализованы и единицы (мс, уд/хв, вд/хв, год, %).
- `oura-sync` теперь тянет ещё `daily_activity` (шаги) и кладёт в `raw.activity`; дыхание/длительность/эффективность берём из `raw.sleepDetail` (без миграции). `oura.ts`: `HealthSnapshot.raw` + select `raw`.
- Корреляции/аналитика — отдельно (Аналитика «по календарю»).

### 🚧 TODO (деплой)
- [ ] `npx supabase functions deploy oura-sync` (добавлен daily_activity)
- [ ] `npx expo start -c` + Reload; во вкладке Здоров'я нажать «Оновити» (подтянуть новые поля)

### 🔜 Осталось из фидбека
- **C. Цикл Марии** — экран дат + расчёт дня/фазы (цикл НЕ из OURA API).

---

## 2026-06-15 (ночь-5) — доводка флоу + укр. склонения

### ✅ Сделано (всё клиентское, деплой не нужен — только Reload)
- **Сворачивание-разворачивание завершённого упражнения** теперь тогл: тап по названию развёрнутой завершённой карточки (с ▲) — сворачивает обратно. Раньше развернуть мог, а свернуть — только через Відновити→Завершити.
- **Невыполненные подходы** (без `logged_at`) исключены из тоннажа/счётчиков подходов/повторов (`workoutStats`); упражнение без единого сделанного подхода не считается. В деталях саммари такие подходы помечены **«не виконано»** и приглушены, без цифр/отдыха.
- **Украинские склонения числительных** — свой хелпер `src/lib/i18n/plural.ts` (не зависит от Intl.PluralRules, которого нет в Hermes): повтор/повтори/повторів, підхід/підходи/підходів. Применено в детали программы и свёрнутой карточке тренировки.

### 🔜 Осталось из фидбека
- **B. OURA — все показатели + расшифровка по тапу.**
- **C. Цикл Марии** — экран дат + расчёт дня/фазы.

---

## 2026-06-15 (ночь-4) — фиксы качества + новый флоу тренировки

### ✅ Быстрый батч (качество программ/упражнений)
- Консервативный матчинг каталога (не форсим совпадение → меньше «левых» названий/категорий; что не уверены — остаётся кастомным с верным именем).
- Короткие названия программ + заголовок детали не обрезается (`...`).
- В пикере кастомные помечаются **«Додано вами»** вместо «Інше».
- Кнопка «Додати вправу» не клипается на части устройств.

### ✅ Новый флоу тренировки (A) — переделан
Миграция `20260615160000_workout_set_logging.sql`: `sets.logged_at`, `workout_exercises.done_at`.
- Подход «сделан» по галочке ✓ (или авто при выставлении RPE) → пишем время, **отдых = разрыв с прошлым сделанным** (показываем на строке). Якорь авто-отдыха = последний `logged_at`.
- Под упражнением **«Додати підхід»** + **«Завершити вправу»**; завершённое **сворачивается** в строку (имя · N підходів · лучший вес×повторы), тап — развернуть, есть «Відновити».
- Убрал общую тупую «Підхід зроблено». `setSetLogged`/`setExerciseDone` в `db/workouts.ts`.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615160000`)
- [ ] `npx supabase functions deploy program-import` (консервативный матчинг + короткий тайтл)
- [ ] `npx expo start -c` + Reload

### 🔜 Осталось из фидбека (по приоритету пользователя: A сделан)
- **B. OURA — все показатели + расшифровка по тапу** (значение/смысл/референсы).
- **C. Цикл Марии** — экран логирования дат + расчёт дня/фазы (цикл НЕ из OURA API).

---

## 2026-06-15 (ночь-3) — кроссфит-блоки + OURA HRV/RHR

### ✅ Сделано
- **Блоки/кластеры в программах** (кола/EMOM/E2MOM/AMRAP/суперсет/интервалы). Миграция `20260615150000_program_blocks.sql` (`program_blocks` + `program_exercises.block_id`). Парсер ИИ теперь группирует упражнения в блоки (тип, label «3 кола»/«EMOM 16», rounds/interval/duration/rest). Деталь программы рендерит блоки с заголовком (акцентная линия) и метой; `groupProgram()` в `db/programs.ts` (legacy без блоков — каждое отдельной группой). Префил тренировки идёт по блокам по порядку.
- **Подсказка про кластеры** в импорте (hint + placeholder): «пиши 3 кола / EMOM 16 / E2MOM / AMRAP / суперсет». i18n `blockTypes.*`.
- **OURA HRV + пульс покоя.** `oura-sync` тянет ещё `usercollection/sleep` (детальный) → `average_hrv` → hrv, `lowest_heart_rate` → rhr (берём long_sleep). Вкладка Здоров'я показывает HRV и Пульс спокою.
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615150000` — блоки)
- [ ] `npx supabase functions deploy program-import oura-sync`
- [ ] Перезапустить Metro `npx expo start -c` + Reload (клиентские фичи: блоки, «Створити…», префил-кнопка — нужны свежий бандл)

### ❓ Цикл для Марии — НЕ из OURA
В публичном OURA API v2 нет эндпоинта фазы цикла. День/фазу считаем сами из дат начала цикла (таблица `cycle_periods` уже есть). Нужен мини-UI логирования дат + расчёт фазы — отдельным шагом.
Поля, которые реально тянем/можем тянуть из OURA: readiness score (+ contributors), sleep score, **HRV (average_hrv)**, **RHR (lowest_heart_rate)**, темп. отклонение, дыхание (average_breath), активность/шаги, SpO2.

---

## 2026-06-15 (ночь-2) — кастомные упражнения + старт из программы

### ✅ Сделано
- **Свои упражнения (безопасно + лимит).** В пикере тренировки при поиске — строка «Створити «…»». Приватность уже даёт RLS (owner_id, is_global=false → видит только владелец). Антиспам: миграция `20260615140000_exercise_daily_cap.sql` — триггер `enforce_exercise_daily_cap` (≤20/день на юзера; **импорт под service_role не лимитируется**, т.к. auth.uid() null). `createCustomExercise` в `db/exercises.ts`, ошибка лимита → алерт `customCapped`.
- **Старт тренировки из программы (префил).** Кнопка «Почати тренування» в детали программы → создаёт сессию и префиллит упражнения/подходы плановыми (вес из канон. кг → активную единицу), RPE/отдых пусты (по факту). `startWorkoutFromProgram` в `db/programs.ts`; `addWorkoutExercise` теперь возвращает id.
- Каталог в промпт импорта — оставили как есть (≈1.5–2k токенов, ~$0.0015/импорт, импорт редкий).
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase db push` (миграция `20260615140000` — триггер лимита)
- [ ] (если ещё не делал) `npx supabase functions deploy program-import` — матчинг каталога + конвертация кг из прошлого захода
- [ ] OURA (Маша генерит PAT): `npx supabase db push` (миграция `20260615120000`) + `npx supabase functions deploy oura-connect oura-sync`, потом вставить токен во вкладке Здоров'я

---

## 2026-06-15 (ночь) — фиксы по фидбеку с устройства

### ✅ Сделано
- **Баг: вход «без пользователя» по deep-link (скан QR).** Гейт был только в `index.tsx`; прямой переход на таб/деталь его миновал. Добавил auth-гард в `(tabs)/_layout.tsx` и на корневые экраны `program/[id]`, `workout/[id]`, `summary/[id]` (нет сессии → `Redirect /auth`, запросы gated на `session`).
- **Матчинг упражнений с каталогом.** Теперь ИИ сам сопоставляет упражнение со списком каталога (передаём каталог в промпт, `catalog_index`) — ловит синонимы и перевод (рус/укр/англ). Чего нет в каталоге — заводим как **кастомное упражнение пользователя** (станет доступно в пикере/аналитике), а не «немає в каталозі». Возвращаем `created_exercises`.
- **kg/lb: настоящая конвертация.** Вес храним канонически в **кг**; хелперы `fromKg/toKg/formatWeight` в `use-unit.ts`. Импорт конвертит числа из единицы профиля в кг; деталь программы показывает с пересчётом (120 кг → 264.6 lb, не «120 lb»).
- **Крупнее шрифт** в детали программы (заголовок упражнения `text-lg`, строки подходов `text-base`).
- Проверка: `tsc` ✅, web ✅.

### 🚧 TODO (деплой)
- [ ] `npx supabase functions deploy program-import` (изменился матчинг + конвертация — миграции новой нет)
- [ ] Перелогиниться/перезагрузить клиент, повторить импорт примера; проверить, что «толчок гири»/«кисть ручкою» теперь сматчены или заведены кастомными.

### ⏭️ Известный хвост
- Лог тренировки/саммари пока хранят вес «как ввёл» (не канонически в кг) — при логировании в одной единице ок; привести к канону при фиче «старт тренировки из программы».

---

## 2026-06-15 (вечер) — ИИ-гейтвей + Программы (#1)

### ✅ Сделано
- **Провайдер-агностик ИИ-гейтвей.** Роутинг по интентам через БД, не хардкод модели.
  - Миграция `20260615130000_ai_gateway_and_programs.sql`: таблица `ai_model_routes` (intent → provider/model + цены, сид: `program_import`/`classify` → GPT-5.4 mini, `coach_chat` → Claude Haiku 4.5, `vision_import` → Gemini Flash off), `ai_limits` (kill-switch, дневной лимит запросов/токенов на юзера, общий $-потолок месяца = 30), security-definer RPC `ai_budget_check` + `ai_record_usage` (учёт в `ai_usage` день/неделя/месяц).
  - Адаптеры `supabase/functions/_shared/ai/`: `openai.ts`, `anthropic.ts`, `gemini.ts` + единый `gateway.ts` (`runIntent`: бюджет → роут → адаптер → учёт расхода). Нет ключа провайдера → он просто «выключен», функция не падает.
- **Программы (#1, §3.7).** Таблицы `programs`/`program_exercises`/`program_sets` (+RLS). Функция `program-import`: текст → GPT-5.4 mini (через гейтвей) → JSON → матч с каталогом → запись. Экран `Програми` (вставка текста → импорт → переход в деталь), `program/[id]` (упражнения/подходы, метка «немає в каталозі» для несматченных). Слой `src/lib/db/programs.ts`. i18n en/uk.
- Проверка: `tsc` ✅, `expo export -p web` ✅.

### 🚧 TODO по ИИ/Программам (деплой + ключи)
- [ ] `npx supabase db push` (миграция `20260615130000`)
- [ ] `npx supabase secrets set OPENAI_API_KEY=...` (обязателен для парсера), позже `ANTHROPIC_API_KEY` (коуч-чат), `GEMINI_API_KEY` (vision)
- [ ] `npx supabase functions deploy program-import`
- [ ] Живой тест парсера на реальном расписании; при кривом JSON — глянуть `parse_failed.raw`
- [ ] Дальше: vision-импорт (фото/скан расписания) на Gemini; «начать тренировку из программы» (префилл сетов); коуч-чат на гейтвее

---

## 2026-06-15

### ✅ Сделано
- **Реактивная смена kg/lb** — единица в общем сторе (`useSyncExternalStore`, `src/lib/use-unit.ts`); смена в Акаунте обновляет экраны мгновенно. Инициализация на старте (`_layout`), обновление из профиля (гейт), онбординга и Акаунта через `setWeightUnit`/`applyUnit`.
- Подтянул работу с дома (табы / RPE / таксономия / каталог 82); проверил: tsc + web-бандл зелёные, миграции в облаке применены.
- **OURA через PAT — заложено (ждёт деплоя + токена).** Решили PAT, не полный OAuth (для 2 юзеров проще).
  - Миграция `20260615120000_oura_pat_and_cycle.sql`: PAT-режим `private.oura_tokens` (refresh/expiry nullable), флаг `profile.oura_connected`, поля `health_snapshots.cycle_day/cycle_phase`, таблица `cycle_periods`, security-definer RPC `store_oura_token`/`get_oura_token` (схема private не экспонирована).
  - Edge Functions: `oura-connect` (валидирует токен у OURA + хранит), `oura-sync` (тянет daily_readiness/daily_sleep → `health_snapshots`).
  - UI вкладки Здоров'я: вставка PAT → подключение → Readiness/Sleep + «Оновити». Слой данных `src/lib/db/oura.ts`.

### 🚧 TODO по OURA (деплой)
- [ ] `npx supabase db push` (применить миграцию)
- [ ] `npx supabase functions deploy oura-connect oura-sync`
- [ ] Позже: Маша генерит OURA Personal Access Token (cloud.ouraring.com) и вставляет во вкладке Здоров'я; отладка с реальным кольцом.
- [ ] HRV/RHR пока не маппятся (только readiness/sleep score + raw) — уточнить поля на реальных данных. Цикл: схема готова, расчёт фазы + сид дат Маши — позже.

### 🔜 Дальше (порядок согласован: 4 → 2 → 1)
1. ~~kg/lb вживую~~ ✅ · ~~OURA заложить~~ ✅ (ждёт деплоя)
2. **Программы + ИИ-импорт** (§3.7) — следующее крупное.
3. Аналитика (фаза 2).

---

## 2026-06-14

### ✅ Сделано
1. **Каталог упражнений + таксономия** (#3)
   - Миграции: `supabase/migrations/20260614120000_exercise_taxonomy.sql` (поля `cluster`/`category` + констрейнты) и `20260614120100_seed_full_catalog.sql` (~82 упражнения, двуязычные, собраны из дневника Маши).
   - **Применены к облаку через `supabase db push`** (сделал пользователь).
   - Пикер упражнений: группировка по кластерам (Верх/Низ/Все тіло/Кор), категория в строке, поиск по названию+алиасам. Файлы: `src/lib/db/exercises.ts`, `src/app/workout/[id].tsx`.
2. **RPE выпадающим выбором** — зоны 🟢1–4 / 🟡5–7 / 🔴8–10 с подсказками вместо ручного ввода. `src/app/workout/[id].tsx` (`RpePicker`).
3. **UX-фиксы пикера:** клавиатура не открывается сама, секция «Часті» сверху (`getRecentExercises`), сворачивание клавиатуры свайпом, сброс строки поиска при открытии.
4. **Нижние табы** (`src/app/(tabs)/`): Тренування / Програми / Аналітика / Здоров'я / Акаунт.
   - Sign out переехал в **Акаунт** (там же язык + единицы).
   - OURA — карточкой во вкладке **Здоров'я** (опционально). Програми/Аналітика — заглушки.
5. **Фикс бага с языком:** язык восстанавливается на старте приложения (`src/app/_layout.tsx` + `src/lib/prefs.ts`), больше не сбрасывается на `en` после релоада.
6. **SPEC обновлён до v0.4** — таксономия/RPE (реализовано), импорт программ ИИ (§3.7) и цикл в OURA (§3.5) зафиксированы как план.

### 🔧 Открытые хвосты (с чего начать завтра)
- [ ] **Телефон не видит изменений, а web (localhost) видит.** Скорее всего Expo Go/dev-клиент держит старый бандл. Пробовать: `npx expo start -c` (чистый кэш), перетряхнуть/Reload на телефоне, проверить что телефон и ПК в одной сети.
- [ ] **Смена единиц kg/lb в Акаунте не «вживую»** — `useWeightUnit()` читает AsyncStorage только на маунте. Сделать реактивным (контекст/стор), чтобы экран тренировки обновлялся сразу.
- [ ] Проверить табы на устройстве: иконки-эмодзи, отступы, что workout/summary открываются поверх табов.

### 🔜 Следующие большие фичи (по приоритету)
1. **#1 Программы + импорт через ИИ** (§3.7): таблицы `programs/program_exercises/program_sets`, экран «Програми», Edge Function парсинга текста/файла → упражнения (файлы не храним).
2. **#2 OURA + цикл** (§3.5): OAuth2 Edge Function, `health_snapshots` + `cycle_day/cycle_phase`, подтяжка readiness/сна; засидить даты цикла Маши (лист «КД»).
3. **Аналитика** (фаза 2): SQL-вьюхи/RPC (тоннаж, объём, 1ПМ, PR) + графики.

### Зацепки
- Бэк решили оставить на **Supabase (SQL) + Edge Functions на TypeScript** — Python не вводим (см. обсуждение; оправдан только под будущий data-science отдельным воркером).
- Данные Маши (упражнения, рекорды, даты цикла) — в её Google-таблице, листы «Тренування2025», «Трені 2026», «Рекорди», «КД».
