# Журнал работы / с чего продолжать

> Живой лог по сессиям. Самое свежее — сверху. Подробности фич — в `SPEC.md`.
>
> Архивы: `docs/progress/2026-07-archive.md` (день-31…день-47), `docs/progress/2026-06-archive.md` (день-30 и старше).

---

## ▶ START HERE (обновляю в конце каждой сессии)

**Старт сессии:** `git pull --ff-only` → прочитать этот блок → при нужде глянуть свежий раздел ниже и `docs/BACKLOG.md` (секция «Порядок»).

- **День-60 (main/backend/OTA prod):** динамометрическая заявка теперь строго одноручная (`left/right`), а борд имеет пять серверных представлений: лучший на приборе, левая, правая, сумма лучших L+R на том же приборе и абсолютный одноручный рейтинг всех приборов. Миграция `20260716160000` и preview OTA runtime 1.0.3, группа `3c245794`, опубликованы; production integration smoke всех режимов/пруфов PASS, временный пользователь удалён. Старые две GM-150 заявки без руки сохранены только в общем/абсолютном рейтингах.
- **День-59 (main/backend/OTA prod):** XF‑300 разделён на две независимые категории `14 mm` и `18 mm`; общей динамометрической таблицы «Усі» больше нет — каждый прибор запрашивается сервером по стабильному `code` до `LIMIT 500`. Миграция `20260716150000` и preview OTA runtime 1.0.3, группа `ef4f6fdb`, опубликованы; старый RPC сохранён для APK до OTA, неактивный общий XF‑300 закрыт триггером. 22 теста, `tsc`, code review ×2 и production smoke зелёные.
- **День-58 (backend prod):** рекорды силы исправлены миграцией `20260716140000`: единая эффективная нагрузка `set_load()` учитывает вес тела, старые собственновесовые `weight=bodyweight` нормализованы, вместо топ‑5 дублей по e1RM строится Pareto-фронтир вес×повторы. На live-данных подтверждено: одноручное подтягивание `95×3` (e1RM `102.125`), швунг `60×4 / 50×5 / 40×15`. Новый APK/EAS Update не нужен; 21 тест, `tsc`, SQL-инварианты и dry-run зелёные.
- **День-57 (backend prod):** импорт программ и прошлых тренировок переключён с исчерпавшего квоту OpenAI на уже настроенный Anthropic `claude-sonnet-4-6`; live-маршрут `program_import` сверен (`enabled=true`, `$3/$15` за 1M токенов). Миграция `20260716130000_program_import_anthropic.sql` применена, новый APK/EAS Update не нужен. Локальный import smoke-runner и README тоже переведены на Anthropic. AI-gateway review — PASS; 21 тест, `tsc`, `node --check`, `git diff --check` зелёные.
- **День-56 (main/backend prod + Android APK готов):** документация и продуктовая модель синхронизированы: `grip` для всех (свободная тренировка + эспандеры/рекорды + лидерборд), private `full/admin` только Мария/Сергей. Fail-closed UI + RLS/OURA gates и unique active-workout index уже в проде. Durable workout scope + debounce/coalesce `UPDATE→LOG` с flush при background/finish; **21 тест зелёный**, `tsc`/7 Edge checks/Expo Doctor 18/18 зелёные. Android preview v1.0.3 build `8c31563a-2af4-4288-b566-c18db4a20ff9` (`c5fd35a`) завершён: https://expo.dev/artifacts/eas/YRLrehT6JGLHj-VLuwrYJ_o82YcGLxeSC7cXuJlhQSk.apk
- **День-54 (свежее):** (а) Аналітика разрезана на 3 суб-таба «Тренування / Рекорди / Відновлення» (пункт 9 ресёрча) + деплинк `/analytics?tab=recovery` из Здоров'я — **в проде**, `eas update` группа **`8f7908cb`**, коммит `bbde4f8`; Сергей подтвердил («табы вижу, всё ок»). (б) По его команде: разбор `ППН1.2.xlsx` + его 17 тренировок → **3 программы насеяны в его акк** (ППН-хвиля подтягиваний, натяжка-день, гиря+еспандери; сидер `scripts/seed-sergey-programs.mjs`), кап программ 3→6 (`d83564b`) — опубликован, `eas update` группа **`cab0758a`**. Подробности ниже.
- **День-53 (всё в проде):** разбор фидбека Сергея по скринам S25 — график тоннажа (даты не переносятся: день + месяц на смене), OURA-фолбэк (частичный день не вытесняет полный, пометка «дані ще підтягуються з хмари»), карточка «Аналітика скоро» → переход в Аналітику, «ІІ»→«ШІ» по всей uk-локали, чипы коуча отцентрованы; **дата-фикс в проде** (одобрен): персональные «Пронація» и «Підтягування на одній руці» Сергея получили cluster=upper (висели в «Інше»). `eas update` группа **`1bc1b157`** (рантайм 1.0.2), коммит `6b6f54a`. Ресёрч конкурентов → `docs/research/2026-07-08-competitor-apps.md` + приоритеты в BACKLOG §10.
- **День-52 (частично подтверждён: скрины аналитики/здоровья ок, клавиатура/авиарежим ещё не проверены):** (а) **клавиатурный порт** — KAV выпилен с экрана тренировки и из `bottom-sheet.tsx`, ручной `paddingBottom = useKeyboardHeight()` (паттерн коуча дня-51, подтверждённый Сергеем на S25); (б) **тоннаж — единый SQL-источник `set_tonnage()`** (миграция `20260708100000` **применена**; вью `workout_summaries` + RPC `analytics_summary_for` пересажены, числа сверены на проде байт-в-байт; клиентская копия `workoutStats` осталась осознанно — offline, перекрёстные комменты); (в) **офлайн-аудит §5**: заявленный в доках expo-sqlite **не используется вовсе** — реальный офлайн = TanStack persist + persisted paused-мутации (AGENTS.md исправлен); **хардненинг**: `durableRetry(3×, backoff)` на 10 durable-ключей (глобального ретрая нет — неидемпотентные insert'ы/платный ИИ), при ошибке оптимистика НЕ стирается (onSuccess-only invalidate), бейдж SyncStatus «не вдалося синхронізувати (N) — повторити», коуч офлайн — явное «немає з'єднання» вместо вечного спиннера и сюрприз-отправки на реконнекте; (г) **допил аналитики §6b** (основные секции жили с прошлых сессий, BACKLOG был стухший — отмечен): «N тренувань за період», по фазам цикла теперь `count× · RPE · тоннаж`, новая связка **«легкість ↔ готовність»** (RPE/тоннаж в дни готовности ≥80 vs <80), `activity_score` в трендах; **середня довжина циклу** на карточке Здоров'я (§4). `eas update` группа **`2c0094c6`** (рантайм 1.0.2). Ревью `/code-review medium` (6 finder-агентов): 7 находок, 4 закрыты, 3 — осознанные трейдофы (см. день-52).
- **День-51 (подтверждён Сергеем на S25):** таб «Головна» выпилен (лендинг `/workouts`), «Лідерборд» прячется тумблером (`profile.show_leaderboard`, миграция `20260707150000`), полиш коуча (иконки + минус KAV). `eas update 4965d81c`. Маше — выключить тумблер у себя.

- **Состояние `main` (2026-07-16):** feature-коммит дня-60 `cbcfb64`; backend и preview OTA `3c245794` уже опубликованы. Expo остаётся SDK 54 (`expo 54.0.36`, `expo-updates 29.0.19`), app/runtime `1.0.3`; нативная пересборка не требуется.
- **День-50 — всё в проде:** (а) аудит безопасности — чисто; defense-in-depth RLS на `private.oura_tokens` (`20260707120000`); (б) **коуч-треды** — миграция `20260707130000`, `coach-chat` с `thread_id`, клиент со списком тредов (коммит `8e30479`; db push + functions deploy + `eas update` группа `e6afc5ff` сделаны); (в) **таб «Головна»** (IA §1, см. секцию дня-50) — JS-only, `eas update` группа `a06541b7`; (г) **фикс клавиатуры на экране тренировки** (KAV — нижние подходы снова доступны) — `eas update` группа `98185b20`. (б)–(г) применяются на 2-м запуске APK **v1.0.1**.
- **Данные:** у Сергея реальные тренировки (№1–15) — при тестах НЕ трогать. Роли на проде: Сергей=admin (S25, APK v1.0.2), Маша=full (**iPhone** — TestFlight в этапе 3 §9). Антиспам 5 заявок/24ч на admin не действует (день-47). `scripts/smoke-notify.mjs` — смоук пушей. **Превью-акк `preview-claude@sporty.local` насеян мок-данными** (OURA 75 дн + цикл + 24 мок-тренировки; сидеры `scripts/seed-preview-health.mjs` + `seed-mock-workouts.mjs`) — для будущих превью-смоуков аналитики.
- **Твои действия:** дважды запустить APK v1.0.3 и проверить в динамометрическом борде режимы `Найкращий / Ліва / Права / Л + П / Рейтинг сили`; новая заявка должна требовать выбор одной руки. Старые `125.5` и `77 кг` без подтверждённой руки намеренно не участвуют в side/sum.
- **Следующее:** добавить полноценные Supabase integration tests для RLS/unique index/рекордного фронтира и транзакционный commit AI-imports. Если Anthropic-импорт даст `parse_failed`, добавить строгий structured output. **Apple/TestFlight — только по явной команде Сергея.**

---

## 2026-07-16 (день-60) — динамометры: левая, правая, сумма и абсолют

### ✅ Сделано

- `leaderboard_entries.hand` хранит только `left/right`; новые dynamometer-вставки и переход в approved без руки блокируются серверным триггером. Старые две approved GM-150 заявки с неизвестной рукой не угадывались и остаются доступны только там, где сторона не влияет на смысл.
- Новый 4-arg `get_leaderboard` серверно строит пять режимов: лучший одноручный результат пользователя на выбранном приборе, левая, правая, сумма лучших L+R одного пользователя на одном приборе с двумя proof URL и абсолютный лучший одноручный результат среди всех приборов. Старые 1/3-arg overload сохранены для чтения старым APK.
- UI получил компактные режимы прибора и отдельную акцентную карточку «Рейтинг сили · усі прилади»; форма требует руку, модерация и собственные заявки её показывают, сумма отображается как `Σ · Л + П` и открывает оба пруфа. en/uk и правила видео обновлены.
- Production migration `20260716160000` применена. Integration smoke создал временного пользователя и пять заявок на GM-150/XF-300, проверил device_all/left/right/sum/absolute, точные L/R proof URL и каскадную уборку — PASS. `tsc`, 22 Jest-теста, JSON/node checks, EAS export и повторные RN/SQL reviews — зелёные.
- Feature-коммит `cbcfb64`; preview OTA runtime 1.0.3: группа `3c245794-4d97-4f6f-9cbb-587cf1087377` (Android+iOS). Нативный билд не нужен.

### ⏭️ Следующее

- Device smoke после второго запуска APK. Если по двум старым GM-150 видео можно достоверно определить руку, назначить её отдельной админской миграцией; без просмотра не угадывать.

## 2026-07-16 (день-59) — XF‑300: отдельные рейтинги 14/18 мм

### ✅ Сделано

- Миграция `20260716150000_split_xf300_handles.sql` добавила активные `xf300_14mm` и `xf300_18mm`, деактивировала неоднозначный `xf300` и сохранила его строку/FK для истории. На момент миграции старых XF‑300 заявок не было.
- Серверный trigger запрещает новые заявки в неактивный прибор даже через старый/модифицированный клиент. Старый `get_leaderboard(text)` сохранён для rollout-совместимости; новый overload фильтрует по `dynamometer_code`/`set_type` до `LIMIT 500`.
- Клиент убрал общий рейтинг «Усі» для динамометров, выбирает конкретный активный прибор и включает стабильный code в query key. Часовой persisted-кэш справочника сброшен ключом `dynamometers/v2`.
- Регрессионный тест фиксирует независимые RPC-фильтры 14/18 мм и fail-closed запрет пустой категории. Mock/smoke-скрипты выбирают только активные приборы; `USER_FLOWS.md` обновлён.
- Production smoke: четыре активные категории в правильном порядке; legacy inactive; старый и новый RPC работают; вставка в старый XF‑300 отклонена. 22 Jest-теста, `tsc`, dry-run, `git diff --check`, два code review — PASS. Feature-коммит `b0e3b05`; preview OTA runtime 1.0.3 — группа `ef4f6fdb-d341-405e-ae7b-62973dea367d`.

### ⏭️ Следующее

- Device smoke после второго запуска APK: четыре чипа приборов, отдельная подача/рейтинг XF‑300 14 и 18 мм, модерация показывает точное название категории.

## 2026-07-16 (день-58) — bodyweight PR и честный весо-повторный фронтир

### ✅ Сделано

- Миграция `20260716140000_record_frontier_and_bodyweight.sql` добавила каноническую `set_load()`: обычное упражнение использует `weight`, собственновесовое — `bodyweight + дополнительный weight`. `set_tonnage()` переведён на тот же источник формулы.
- Старые собственновесовые подходы, созданные до миграции bodyweight и хранившие полный вес тела в `sets.weight`, безопасно нормализованы в `weight=null` по узкому условию `weight = profile.bodyweight`.
- `analytics_summary_for()` теперь оставляет один лучший результат на нагрузку и недоминируемый Pareto-фронтир: `60×4` убирает `60×3/60×1/56×1`, но сохраняет настоящие PR `50×5` и `40×15`.
- Production db push выполнен. Read-only сверка: одноручное подтягивание `95×3`, e1RM `102.125`; швунг `60×4`, `50×5`, `40×15`. В миграцию встроены SQL-инварианты формулы и фронтира; dry-run, 21 Jest-тест, `tsc`, `git diff --check` зелёные.

### ⏭️ Следующее

- Визуальный smoke экрана рекордов после истечения 30-секундного staleTime/перезапуска приложения; затем перенести сценарии рекордов в постоянные Supabase integration tests.

## 2026-07-16 (день-57) — AI-импорт переведён с OpenAI на Anthropic

### ✅ Сделано

- Миграция `20260716130000_program_import_anthropic.sql` переключила общий intent `program_import` на `anthropic / claude-sonnet-4-6` с ценами `$3/$15`; она применяется и к импорту программ, и к импорту прошлых тренировок.
- Миграция применена в production через `npx supabase db push`; live-строка проверена read-only: маршрут включён и указывает на Sonnet 4.6. `ANTHROPIC_API_KEY` присутствует среди Edge secrets; значение не извлекалось.
- `scripts/test-import-prompt.mjs` и `scripts/import-fixtures/README.md` синхронизированы с боевым Anthropic Messages API.
- AI-gateway reviewer: PASS, блокеров по бюджету/ключам нет. Верификация: 21 Jest-тест, `npx tsc --noEmit`, `node --check scripts/test-import-prompt.mjs`, `git diff --check` — зелёные.

### ⏭️ Следующее

- Один реальный импорт прошлой тренировки с устройства. При редком `parse_failed` заменить JSON-only инструкцию на строгий structured output Anthropic.

## 2026-07-16 (день-56) — актуализация продукта, private-гейты, P0 workout и тесты

### ✅ Сделано
- `git fetch --prune origin`: локальная база до изменений совпадала с GitHub (`33008cb`, 0/0). Существующую локальную запись дня-55 и `.agents/.codex` не затирали.
- Полностью актуализированы `SPEC.md`, `BACKLOG.md`, `USER_FLOWS.md`, `README.md` и правило ролей в `AGENTS.md`; убраны старые утверждения про online-only, один Coach thread и отсутствие EAS.
- Роль теперь fail-closed в UI; прямые private deep-links возвращают grip в Workouts. RLS сужает Programs/Health/Cycle/Coach data до `full/admin`; `oura-connect`/`oura-sync` получили server role gate до токена/внешнего запроса.
- Read-only preflight подтвердил prod: `kylmariya@gmail.com=full`, `gonenko1995@gmail.com=admin`, preview=grip; незавершённых тренировок нет. Скрипт `scripts/preflight-private-access.mjs` ничего не меняет.
- P0 workout: локальный resume guard, unique partial index «одна active на user», холодный cache ждёт список; при cross-device `23505` phantom удаляется и открывается серверная active. Offline finish сразу обновляет detail+summary cache.
- Durable операции одной тренировки сериализованы общим TanStack scope (ADD/UPDATE/LOG/DELETE/структура/FINISH, включая START с динамическим client UUID); старый ответ не обгоняет новый. Пустой set meta отправляется `null`, а не `undefined`.
- Jest по официальной схеме Expo SDK 54 (`jest-expo` + RNTL), mocks AsyncStorage/NetInfo; 21 unit/integration-style тест: роли, active/reconciliation, QueryClient offline-finish, scope, debounce/flush/order `UPDATE→LOG`, workout metrics `both×2`, cycle boundaries, kg/lb.
- P2 debounce закрыт: 400 мс coalesce на set input, но durable enqueue всегда `UPDATE` раньше auto-`LOG`; pending значения flush'ятся при blur, background, unmount и перед finish/collapse. Первый review нашёл обратный порядок — исправлен; повторные RN/security review без P0/P1.
- Patch lane внутри SDK54: `expo 54.0.36`, `expo-updates 29.0.19`, прямой peer `expo-asset 12.0.13`; Expo 57 не применялся. App/runtime `1.0.3`, потому что native patch нельзя OTA-отправлять в старый runtime 1.0.2. `npm audit --omit=dev`: high=0, остаются 17 moderate Expo-tooling, force-fix требует Expo 57.
- Верификация: `npm test` 21/21 ✅; `npx tsc --noEmit` ✅; `npm run check:functions` 7/7 ✅; Expo Doctor 18/18 ✅; Android production export ✅; `git diff --check` ✅.
- Commit/push: feature `cc9e308`, EAS upload hygiene `c5fd35a`; `.easignore` исключает локальные `.agents/.codex` из cloud archive.
- Prod deploy выполнен в безопасном порядке: preflight (Maria=full, Sergey=admin, preview=grip, active=0) → `oura-connect` → `oura-sync` → migration `20260716120000`; remote migration list совпадает, повторный preflight чистый.
- Android preview build v1.0.3/runtime 1.0.3 завершён: EAS id `8c31563a-2af4-4288-b566-c18db4a20ff9`, commit `c5fd35a`, fingerprint `6e5d3894…`, APK https://expo.dev/artifacts/eas/YRLrehT6JGLHj-VLuwrYJ_o82YcGLxeSC7cXuJlhQSk.apk. OTA в runtime 1.0.2 не публиковался.

### 🚧 TODO (деплой)
- Установить APK v1.0.3 на S25 и пройти device smoke из START HERE. Backend дня-56 уже в prod.
- Настоящий persist/hydrate test и Supabase integration suite остаются следующим слоем; Expo 57 — только отдельным согласованным апгрейдом.

### ⏭️ Следующее
- Добавить Supabase integration suite: grip deny/private allow/foreign deny, child RLS, второй active insert=`23505`, следующий start после finish.
- Транзакционный commit program/workout AI-imports; затем общий query error/retry UX.
- После safety — starter program catalog и active-program progress. Cycle edit/delete UI остаётся открытым хвостом.

## 2026-07-13 (день-55) — контроль синхронизации и хвостов

### ✅ Сделано
- `git pull --ff-only`: `main` уже совпадает с `origin/main`; новых изменений нет. Рабочее дерево кода чистое; локальные неотслеживаемые служебные каталоги `.agents/` и `.codex/` не менялись.
- Сверены `PROGRESS.md` и `BACKLOG.md`: ближайший продуктовый шаг — стартовый каталог программ, но содержание 6–10 кураторских программ нужно согласовать с Сергеем до генерации.

### ⏭️ Следующее
- После согласования состава каталога: спроектировать многодневную модель программы и экран топ-3 + «показать все».
- Отдельно закрыть ручные смоуки S25 (клавиатура, авиарежим, коуч офлайн, суб-табы/деплинк) и перенести реальные тренировки Маши вместо моков.

## 2026-07-09 (день-54) — Аналітика: суб-табы + программы Сергею (ППН 1.2 + композиция логов)

Первый пункт рекомендованного порядка из ресёрча дня-53: экран Аналітики был одной лентой из 6 секций («статистики дохуя» — фидбек Сергея). Верификация: `tsc` ✅; веб-превью (превью-акк, 375px) — все три таба отрисованы и переключаются, деплинк из Здоров'я, консоль чистая (4 key-warning в логе — артефакты HMR промежуточных правок, на свежей загрузке не воспроизводятся). Ревью `/code-review medium` (8 finder-углов): 7 находок → 5 закрыто, 2 осознанных трейдофа.

### ✅ Сделано
- **Суб-табы (analytics.tsx):** «Тренування» (статы + тоннаж + календарь) / «Рекорди» (кластеры + хват) / «Відновлення» (OURA-карточки + кореляції + чипы 30/90/180 — период правит весь таб). Переключатель — общий `Segmented` (тот же, что борд/акаунт/онбординг), скролл к верху при смене таба. Все данные/мемо-расчёты остались общими — переключение табов ничего не перезапрашивает.
- **Гейт «Відновлення» стабильный (ревью-находка, дедлок):** таб виден при наличии OURA-снимков вообще (`hasOura`), а НЕ по результатам окна range — иначе у юзера с данными старше 30 дн прятался и таб, и чипы 90/180, которыми данные можно раскрыть. Глобальный empty-гейт переведён на тот же `hasOura`; пустое окно при живом табе — пометка «немає даних OURA за цей період — спробуй довший діапазон».
- **Деплинк на суб-таб:** `/analytics?tab=recovery|records|training` (`useLocalSearchParams`, параметр сбрасывается после применения — повторный переход срабатывает). Карточка «Кореляції — в Аналітиці» (health.tsx) ведёт прямо на «Відновлення» (раньше падала на дефолтный таб, где корреляций нет).
- **Ревью-фиксы:** hand-rolled сегмент-контрол заменён общим `Segmented` (src/components/segmented.tsx уже существовал); дубль empty-карточки ×3 → локальный `EmptyCard`; копи `recordsEmpty` переписан (не врёт юзеру с подходами, но без рекордов). Ключи `tabTraining`/`recordsEmpty`/`recoveryEmptyRange` en+uk.
- **Осознанные трейдофы (зарепорчены, не чинились):** рекорды/календарь больше не совместны на одном скролле (в этом и смысл разгрузки); OURA-only юзер без тренировок лендится на полупустой «Тренування» (оба реальных юзера с тренировками; главный путь закрыт деплинком из Здоров'я); сброс скролла при переключении (стандарт для сегментов, спасает от стейл-оффсета на коротком табе); `activeTab`-алиас поверх `tab` оставлен (derive-during-render вместо эффекта-синка).
- **BACKLOG §10:** пункт «Аналітика — плотность» закрыт с описанием решения.

### ✅ Деплой (по команде «делай»)
1. `eas update --channel preview` — группа **`8f7908cb`** (рантайм 1.0.2, android+ios), коммит `bbde4f8`. Миграций нет.
2. `git push`.

### 🚧 TODO (устройство)
S25, 2-й запуск после апдейта: (1) три суб-таба (на твоём акке без OURA — только «Тренування»/«Рекорди»); (2) на акке Маши — таб «Відновлення» с корреляциями; (3) тап по «Кореляції — в Аналітиці» на Здоров'я открывает сразу «Відновлення».

### ✅ Сделано (продолжение сессии): программы Сергею по его команде
Сергей скинул `ППН1.2.xlsx` (программа подтягиваний: 9 недель × 3 дня, волновая, лестницы 2-3-5 «с резиной» + тяжёлые сингли с весом 1-3ПМ на дне 2, тест на нед. 9) — «проанализируй + сделай пару программок по моим тренировкам, новичковые дрочи не надо».
- **Разбор его 17 тренировок (05.05–05.07, read-only сервис-ключом):** 4 устойчивых паттерна — (1) кисть на лямках + натяжка (пирамиды 11→32 кг, уже покрыт его ручной программой «Кисть+луч»); (2) натяжка через кисть пирамида + статика 28 кг 22-26с по сторонам + відведення тройками + бок + пронация; (3) гиря (швунг с разгоном до синглов 60 кг + рывок) + діп-сет пирамида эспандеров (HG200→CoC2.5→CoC3×1→бек-оф); (4) подтягивания (3×8 RPE3, макс 15, одноручные сингли).
- **Насеяно 3 программы в его акк** (`scripts/seed-sergey-programs.mjs`, dry-run → вставка → read-back верификация; у эспандеров в meta — ровно те gripper_id, что он логировал): «Підтягування — хвиля 9 тижнів (ППН 1.2)» (3 дня-вправи, недельная волна в нотатках каждой), «Натяжка: піраміда + статика + бок + пронація», «Гиря + еспандери (сингли + діп-сет)». Итого у него 4 программы.
- **Кап программ 3 → 6** (`programs.ts`, коммит `d83564b`, `tsc` ✅): одна «программа» в текущей модели = один день, недельный сплит съедает 3-4 слота; при 4 программах старый кап блокировал создание новых. UI берёт число из константы, серверного капа нет.

### ✅ Деплой (по команде «делай»)
- Кап 3→6 опубликован: `eas update` группа **`cab0758a`** (рантайм 1.0.2, android+ios), коммит `8cbb0d6`. Android update id `019f45e6-46e4-7ae6…` (2-й запуск апки).

### ⏭️ Следующее
- Пункт 1 ресёрча: **стартовый каталог программ** — 6–10 куратор-программ как JSON-сиды (та же структура, что юзерские), экран по Hevy (топ-3 под профиль + «показать все»). Состав/контент программ (новичковая линейка, CoC-прогрессия, пронация/натяжка, «первое подтягивание», женская с циклом) — согласовать с Сергеем до генерации. Учесть урок дня-54: нужна модель «многодневная программа» (сейчас 1 программа = 1 день) — связка с §2 «прогрес по програмі».

---

## 2026-07-08 (день-53) — фидбек смоука S25: график дат, OURA-фолбэк, ШІ, дата-фикс кластеров

Сергей прогнал часть смоука дня-52 на S25 (скрины Аналітики/Здоров'я/Коуча — Машин акк) и дал фидбек. Верификация: `tsc` ✅; веб-превью (превью-акк): график на 375px/десктопе, карточка-переход, чипы, «ШІ» — скрины сняты; логика OURA-фолбэка прогнана на 6 синтетических сценариях (мутить прод-моки для живой проверки не стал). Клавиатура/авиарежим дня-52 остаются неподтверждёнными.

### ✅ Сделано (`6b6f54a`)
- **График тоннажа (analytics.tsx):** подписи «03.06» не влезали в колонку (12 баров) и переносились — теперь день всегда, месяц (`черв`/`лип`, локализованный short) второй строкой и только на смене месяца.
- **OURA, частичный день (oura.ts + health.tsx):** `getLatestSnapshot` теперь приоритизирует день с утренними ОЦЕНКАМИ (readiness/sleep score) — частичный сегодняшний (детальный сон приходит раньше оценок; показывался «сон 0:13, пульс 0») не вытесняет полный вчерашний; возвращает `{snap, pendingDate}`, при более свежей неполной строке — пометка `health.pendingCloud` вместо `stale`. Ключ кэша `['oura-snapshot', userId, 'v2']` (форма сменилась; префикс-инвалидации живы).
- **Здоров'я:** стухшая карточка «Аналітика скоро» → карточка-переход «Кореляції — в Аналітиці →» (`router.push('/analytics')`); мёртвые ключи `analytics.soonTitle/soonBody` и `health.soonTitle/soonBody` выпилены из обеих локалей.
- **uk-локаль:** «ІІ» → «ШІ» (заголовок коуча, импорт, ошибки лимитов — 5 строк); чипы-подсказки коуча — `text-center` (двухстрочный чип выглядел криво).
- **Дата-фикс в проде (одобрен Сергеем):** персональные упражнения Сергея «Пронація» (cluster←upper, forearms, armwrestling — копия глобальной) и «Підтягування на одній руці» (cluster←upper) — обе висели в «Інше» в рекордах. У Маши ещё 11 персональных без кластера, но 0 использований — не трогал. Глобальный каталог был корректен (сид `20260614120100` ставит upper).
- **BACKLOG:** §2 + базовые программы-шаблоны и прогресс по программе; новый §10 (реальные данные Маши вместо моков, ресёрч похожих апок, плотность Аналітики).
- **Ресёрч конкурентов** (Hevy/Strong/Fitbod/Boostcamp/Juggernaut…) — запущен фоновым general-purpose агентом по слабым местам (программы-контент, прогресс по программе, IA аналитики); итоги → BACKLOG §10.

### ✅ Деплой (по команде «делай»)
1. `eas update --channel preview` — группа **`1bc1b157`** (рантайм 1.0.2, android+ios), коммит `6b6f54a`. Миграций нет (дата-фикс — точечный UPDATE сервис-ключом).
2. `git push`.

### 🚧 TODO (устройство)
S25, 2-й запуск: (1) график тоннажа — даты читаются, месяц на смене; (2) «Твій ШІ-коуч» + ровные чипы; (3) утром до синка OURA — пометка «дані ще підтягуються», числа за последний полный день; (4) рекорды: Пронація и одноручные подтягивания в «Верх тіла». Плюс недоигранный смоук дня-52: клавиатура, бейдж синка в авиарежиме, коуч офлайн.

### ⏭️ Следующее
- Итоги ресёрча → приоритизировать базовые программы + прогресс по программе (§2).
- Перенос реальных данных Маши (BACKLOG §10) — отдельная сессия.

---

## 2026-07-08 (день-52) — оркестр-сессия: клавиатура, тоннаж→SQL, офлайн-аудит+хардненинг, допил §6b

Сергей подтвердил день-51 на S25 («всё ок») и дал скоуп «всё» — сессия шла оркестром: два фоновых агента (аудит-исследователь + хардненер в git-worktree) параллельно с основной работой. Верификация: `tsc` ✅; веб-превью с реальным логином и данными (превью-акк насеян с одобрения Сергея); смоук миграции на проде (числа тоннажа байт-в-байт до/после); `/code-review medium` (6 finder-агентов по 8 углам) — 7 находок, 4 закрыты, 3 осознанных трейдофа. Клавиатурная часть — подтвердится на устройстве.

### ✅ Сделано
- **Клавиатурные хвосты (разблокированы девайс-подтверждением коуч-фикса):** `workout/[id].tsx` и `bottom-sheet.tsx` — KAV выпилен, низ прижат вручную `paddingBottom = useKeyboardHeight()`; в bottom-sheet `maxHeight` теперь от видимой части экрана `(окно − клава) × 0.88` (процентный мог выталкивать лист за экран при открытой клавиатуре).
- **Тоннаж → один SQL-источник:** миграция `20260708100000` — immutable-функция `set_tonnage(weight, reps, meta, bodyweight_load, bodyweight)`; вью и RPC пересажены на неё (в RPC ушёл лишний guard: подход с reps И duration_sec теперь считается по reps — выровнено с вью/клиентом, задокументировано в шапке миграции). Гранты по паттерну дня-49 (`revoke from public` + точечные). Клиентская копия `workoutStats` остаётся (offline) — перекрёстные комменты в обе стороны.
- **Офлайн-аудит §5 (агент, read-only):** expo-sqlite из доков — фантом (0 импортов); реальный механизм — TanStack persist (AsyncStorage) + persisted paused-мутации, durable — только 10 ключей логирования тренировки. Топ-3 дыры: (1) провал доигрывания молча стирал запись (retry=0 + invalidate по ошибке), (2) НЕ-тренировочные записи не переживают рестарт офлайн, (3) коуч офлайн — вечный спиннер + сюрприз-отправка. Полный отчёт с таблицей флоу — в сессии; AGENTS.md (стек) исправлен по факту.
- **Офлайн-хардненинг (агент в worktree, вмержен):** дыры 1 и 3 закрыты — `durableRetry` (3×, эксп. backoff, cap 30с) на 10 durable-ключей (глобального ретрая мутаций НЕТ намеренно — неидемпотентные insert'ы и платный ИИ); `MutationCache.onError` — след сбоя в консоли; при ошибке оптимистика НЕ затирается (onSuccess-only invalidate); бейдж `SyncStatus` — состояние «не вдалося синхронізувати (N) — повторити» с ручным перезапуском упавших мутаций; коуч: `onlineManager.isOnline()`-гейт в `send()` → «немає з'єднання» (строки en+uk), сюрприз-реплей из persist невозможен (нет mutationFn-дефолта). Импорты программ/тренировок — явный `retry: 0` (платный ИИ). Дыра 2 — в кандидаты (см. Следующее).
- **Допил аналитики §6b (сами секции жили с прошлых сессий — BACKLOG был стухший, отмечен):** «N тренувань за період»; по фазам цикла — `count× · середній RPE · середній тоннаж` вместо голых количеств; новая связка «легкість ↔ готовність» — RPE/тоннаж в дни готовности ≥80 vs <80 (порог — граница optimal-зоны OURA, обе корзины от 2 тренировок с RPE); `activity_score` в трендах «Відновлення». **Середня довжина циклу** на карточке Здоров'я (§4): диффы соседних отметок «день 1», правдоподобное окно 15–60 дн, последние ≤6 циклов; ключ кэша общий с корреляциями. Строки en+uk.
- **Превью-сид (одобрено Сергеем):** `scripts/seed-preview-health.mjs` (75 OURA-снимков с чередованием high/low готовности + 3 старта цикла + флаги профиля) + штатный `seed-mock-workouts.mjs --user … --count 24` на превью-акк — новые секции проверены на живых данных (скрины сняты).
- **Ревью-фиксы (4 из 7 находок):** стухшие комменты про убранный глобальный `retry:3` (3 файла) переписаны; дифф дат по YMD сведён в `daysBetween()` (cycle.ts; фаза/статус/средняя длина; заодно floor→round — floor терял сутки на переводе часов); ease-карточки дедуплицированы; `useKeyboardHeight(enabled)` — закрытый BottomSheet не подписывается на клавиатуру.
- **Осознанные трейдофы (зарепорчены, не чинились):** error-мутация не переживает рестарт (бейдж теряется; фантом смывается ближайшим рефетчем — постоянного расхождения нет); `retryFailed` без капа попыток + semi-private `MutationCache.build/remove` (API сверен с v5.101); клиентская копия тоннажа без теста (тест-инфры в проекте нет).

### ✅ Деплой (по команде «делай всё»)
1. `npx supabase db push` — `20260708100000` применена; смоук на проде: вью (5743/2503) и RPC (76.3к, те же столбики) — байт-в-байт как до миграции.
2. `eas update --channel preview` — группа **`2c0094c6`** (рантайм 1.0.2, android+ios), коммит `69df32b`.
3. `git push`.

### 🚧 TODO (устройство)
S25, 2-й запуск после апдейта: (1) клавиатура на экране тренировки и в боттом-шитах — нет стейл-гепа после закрытия; (2) бейдж синка в авиарежиме (офлайн → лог → реконнект); (3) коуч в авиарежиме — «немає з'єднання», не спиннер; (4) на акке Маши — новые секции корреляций с её реальными OURA-данными.

### ⏭️ Следующее
- Кандидат: durable-паттерн (mutationKey + defaults) для НЕ-тренировочных записей — профиль/правки программ/цикл/заявка борда теряются при рестарте офлайн (дыра №2 аудита).
- OURA OAuth (§6c) — ждёт живого кольца; Apple/TestFlight — только по явной команде.

---

## 2026-07-07 (день-51) — фидбек с устройства: минус «Головна», тумблер лидерборда, полиш коуча

Фидбек Сергея по v1.0.2 (скрин коуча + «Головна — хуйня, Маше не нужен лидерборд по эспандерам»). Решения через опрос: Головну убрать совсем; лидерборд — весь таб по тумблеру; геп коуча — «после клавиатуры» (стейл-паддинг KAV). Верификация: `tsc` ✅, веб-превью (логин, все экраны, скрины) ✅; клавиатурная часть — только на устройстве. Ревью `/code-review medium` (8 углов) прогнано, находки закрыты.

### ✅ Сделано
- **Таб «Головна» выпилен** (жил 3 коммита, день-50): `home.tsx` удалён, лендинг снова `/workouts` (`index.tsx`/`onboarding.tsx`), ключи `hub.*`+`tabs.home` убраны из en+uk, осиротевший `topPerKey` убран из `db/analytics.ts`. Неймспейс `home.*` в i18n — это экран «Тренування», не тронут.
- **Тумблер «Показувати лідерборд»** (Акаунт → Налаштування): `profile.show_leaderboard` (миграция `20260707150000`, default true), `get/setShowLeaderboard` + общий хук `src/lib/use-show-leaderboard.ts` (один queryKey/staleTime на таб-гейт и свитч), оптимистичный флип (таб исчезает мгновенно), `href:null`-гейт в `(tabs)/_layout.tsx`. Пока флаг не загружен — таб виден (дефолт-большинство, не мигает). Строки en+uk.
- **Коуч, шапка:** «нова розмова»/«розмови»/шестерёнка теперь в одинаковых 36px-боксах с ровным шагом и общим центром (шестерёнка была голой 22px-иконкой → кривые отступы). Проверено пиксельно в превью (cx с шагом 36, cy совпадает).
- **Коуч, «огромный геп после клавиатуры»:** KeyboardAvoidingView убран — его анимированный паддинг на Android оставался после закрытия клавиатуры. Теперь низ прижат вручную: `useKeyboardHeight()` (новый, в `use-keyboard-visible.ts`; `useKeyboardVisible` теперь производный) → `paddingBottom = высота клавы + 12 | таббар + 12`. iOS: добавлен `keyboardWillChangeFrame` (emoji-клавиатура/QuickType меняют рамку без повторного willShow). Экран тренировки НЕ трогал — его KAV-фикс дня-50 подтверждён рабочим, менять вслепую без девайса не стал.
- **Ревью:** подтверждён и закрыт fail-open в `getShowLeaderboard` (глотал ошибку → дефолт true мог перезаписать выключенный тумблер в persisted-кэше; теперь throw — кэш переживает сбой сети); query вынесен в общий хук (копии успели разъехаться по staleTime). Остальные углы чистые.
- **Хозяйство:** PROGRESS.md >80KB → день-39…45 перенесены в `docs/progress/2026-07-archive.md`. Для веб-превью заведён тест-аккаунт `preview-claude@sporty.local` (grip, через обычную форму регистрации) — можно удалить, но удобен для будущих превью-смоуков.

### ✅ Деплой (по команде «делай»)
1. `npx supabase db push` — миграция `20260707150000` применена.
2. `eas update --channel preview` — группа `4965d81c` (рантайм 1.0.2, android+ios), коммит `5956442`.
3. Тумблер прогнан end-to-end на превью против прода: выкл → таб «Борд» исчез мгновенно → пережил очистку persisted-кэша + перезагрузку (значение из БД) → вкл обратно.

### 🚧 TODO (устройство)
На S25 (2-й запуск после апдейта): геп коуча после открытия/закрытия клавиатуры ушёл, иконки шапки ровные, лендинг без Головної; на аккаунте Маши выключить тумблер лидерборда.

### ⏭️ Следующее
- Если на тренировочном экране/боттом-шитах после клавиатуры тоже виснет отступ — портировать `useKeyboardHeight` туда же (тот же класс бага, кандидаты из ревью).

---

## 2026-07-07 (день-50) — сквозной аудит безопасности + коуч-треды + таб «Головна» (IA §1)

### ✅ IA-переезд: таб «Головна» (today-хаб, BACKLOG §1/§8) — продолжение сессии
Решения Сергея: все текущие табы остаются, Головна добавляется первой (лендинг); Здоров'я — условный таб как есть; история — секция на Головній + таб Тренування.
- **`(tabs)/home.tsx`** (роут `/home` — `(tabs)/index.tsx` конфликтовал бы с корневым гейтом): снимок дня (готовність/сон OURA + день/фаза циклу — только у кого есть данные, тап → Здоров'я), «Продовжити тренування», «Почати тренування» (общий хук), «Почати з програми» (скрыт у grip), 🏆 последний топ-1 рекорд (тап → Аналітика), 3 останні тренування + «Вся історія» → таб Тренування. Гейты `index.tsx`/`onboarding.tsx` редиректят на `/home`.
- **Общие ключи кэша** с Тренуваннями/Здоров'ям/Аналітикою (одни данные — ноль дублей запросов); почти статичные флаги и тяжёлый RPC сводки на лендинге — `staleTime` 30 мин (инвалидации `['analytics']` всё равно освежают после финиша/удаления/импорта).
- **Ревью `/code-review medium`** (8 углов, 1-vote verify): подтверждены и починены — пустая карточка «Сьогодні» при null-скорах OURA; UTC-vs-локальный день в stale-бейдже (`localYmd` в `lib/dates.ts`, health.tsx переведён туда же); дубль оптимистичного старта (`lib/use-start-workout.ts` — единственное место сида кэшей, workouts.tsx переведён); дубль `humanDate` (→ `lib/dates.ts`); третья копия инварианта «первая строка = топ-1» (→ `topPerKey` в `db/analytics.ts` с контрактом сортировки RPC). Опровергнуто: «сдвиг дня даты рекорда» (date = timestamptz, не date-only). Строки `tabs.home`+`hub.*` en+uk; `tsc` зелёный.

### ✅ Сквозной аудит безопасности (пентест, BACKLOG §5) — критичного не найдено
Прошёл всю поверхность заново (не полагаясь на память дней-46/48/49, сверял по коду):
- **RLS**: у всех 24 public-таблиц включён; политики owner-scoped корректны. `user_roles`/`ai_limits`/`ai_model_routes`/`ai_usage` — только SELECT для клиента (нельзя самоповыситься до admin, поднять свой кап, флипнуть kill_switch, накрутить usage). `leaderboard_entries`: insert форсит `pending`+пустую модерацию, update только пока `pending` (нельзя самоапрувнуться или править одобренное), модерация — admin-RPC.
- **Definer-функции**: локдаун дня-49 (`20260704120000`) — последняя миграция, `revoke from public`+точечные гранты; оверлоадов не осталось (старый 4-арг `ai_record_usage` дропнут перед 5-арг); все оверрайды `get_leaderboard(text)` держат сигнатуру → локдаун бьёт по финальной.
- **Edge-функции**: все верифицируют JWT (`auth.getUser()`, userId из токена, не из тела); ИИ-функции гейтят роль (`hasAiAccess`); инпут-капы (coach 2000, импорты 12000, transcribe 8МБ); биллинг серверный. `transcribe` считает кост по размеру файла, НЕ по клиентскому `durationSec` (фикс инцидента $89M на месте). OURA-токен не возвращается/не логируется. Ключи серверные, в клиенте (`src/`) секретов нет, коммитится только publishable-ключ.
- **Единственная находка (defense-in-depth, не дыра)**: `private.oura_tokens` без RLS — защищён только тем, что схема `private` не в exposed schemas (один тумблер в Settings→API от утечки). Миграция `20260707120000` включает RLS без политик = deny-all для клиентских ролей, service_role (definer-функции) обходит → поведение не меняется.

### ✅ Коуч-треды (§3, хвост дня-31)
Было: один тред на юзера (coach-chat брал последний/создавал). Стало — мультитред:
- **Миграция `20260707130000`**: `ai_threads` +`title` (сервер ставит из первого сообщения, ≤60 симв.) +`updated_at` (бампится на каждое сообщение) + индекс `(user_id, updated_at desc)`. `ai_threads` уже RLS-owner-scoped — новые колонки покрыты.
- **`coach-chat`**: принимает опц. `thread_id` (проверка владения `id+user_id`, чужой не откроет), без него — заводит новый тред; бампит `updated_at`; возвращает `{ reply, thread_id }`. Бюджет/история(24)/капы не тронуты — подтверждено `ai-gateway-reviewer`.
- **Клиент** (`db/coach.ts` + `(tabs)/coach.tsx`): `listCoachThreads`, per-thread `listCoachMessages(threadId)`, `sendCoachMessage(text, threadId)→{reply,threadId}`. В шапке иконки «нова розмова» (✎) и «розмови» (⏰→bottom-sheet со списком тредов, активный подсвечен). **Дата-разделители** в ленте (Сьогодні/Вчора/дата, en/uk). Новый тред засевается в кэш в `onSuccess` (иначе смена ключа запроса мигала полноэкранным спиннером). Строки en+uk (`newChat/history/noThreads/today/yesterday`).

### ✅ Фикс клавиатуры на тренировке + блюр-таббар (продолжение сессии, фидбек со скрина)
- **Клавиатура прятала нижние подходы** (`workout/[id].tsx` был без KAV — edge-to-edge SDK 54 не ресайзит окно): KAV `behavior=padding` + кнопка «Додати вправу» прячется пока печатают (`src/lib/use-keyboard-visible.ts`). Коммит `222724d`, `eas update 98185b20` (рантайм 1.0.1).
- **Блюр-таббар** (хвост дня-49): absolute-бар + BlurView (`dimezisBlurView` на Android — иначе плоская плашка), высота бара — единый источник `src/lib/tab-bar.ts`, отступы контента на всех 7 таб-экранах; инпут коуча над баром (при клаве бар прячется — отступ снимается). **version → 1.0.2** (expo-blur нативный) → билд `7bfe896b`. Коммит `3fa26f3`.

### 🚧 TODO (устройство)
Дождаться билда `7bfe896b` → APK v1.0.2 на S25 → смоук: блюр под баром при скролле, ничего не прячется под баром на 7 табах, инпут коуча/клавиатура, экран тренировки с клавой, коуч-треды, Головна.

### ⏭️ Следующее
Устройство: смоук v1.0.1 (день-49) + коуч-треды. Дальше — блюр-таббар (с девайсом), навбар edge-to-edge, IA-переезд вкладок.

---

## 2026-07-04 (день-49) — добить хвосты аудита + security-фикс грантов + пересборка APK v1.0.1

Продолжение дня-48 («добиваем все хвосты и пересборку»). По ходу работы над `delete_account` **всплыла серьёзная пре-существующая уязвимость** и была закрыта. Верификация: `tsc` ✅, `deno check` (новый `npm run check:functions`, все 7 функций) ✅, `ai-gateway-reviewer` — блокеров нет, живые смоуки на проде (coach `get_records`, `delete_account` на одноразовом юзере, грант-локдаун по трём ролям).

### ✅ Серверные хвосты (миграции `20260704110000`, `20260704120000`; функции задеплоены)
- **`personal_records` ВЫКИНУТА** (пуста, никто не писал): рекорды коуча теперь из `sets` через `analytics_summary_for(p_user)` — та же функция, что питает экран аналитики (`get_analytics_summary` — тонкая обёртка над ней по `auth.uid()`). Живой прогон коуча подтвердил: «топи» + грип-настройки (deep/CoC) озвучиваются верно.
- **`delete_account()` RPC** (Play Store, этап 4 §9): security definer, удаляет строго `auth.uid()` из `auth.users` → каскад по всем таблицам; `leaderboard_entries.verified_by` переведён на `on delete set null` (апрувы переживают уход модератора). Смоук на одноразовом юзере: каскад чистый.
- **🔴 SECURITY: закрыта дыра в грантах.** `revoke execute … from anon, authenticated` НЕ снимает дефолтный грант роли `PUBLIC` (её члены — и anon, и authenticated). Итог: ВСЕ security-definer функции были вызываемы анонимно по публичному anon-ключу. Проверено на проде — anon мог: `get_oura_token(любой uuid)` → **вытащить чужой OURA access-token** (прямое нарушение «ключи только на сервере»), `store_oura_token` → перезаписать, `analytics_summary_for/get_analytics_summary` → **читать чужие тренировки**, `ai_budget_check/ai_record_usage` → читать/**накручивать чужой ИИ-бюджет** (упереть юзера в кап). Фикс (миграция `120000`): `revoke … from public, anon, authenticated` → явный `grant` только нужной роли (service_role для серверных, authenticated для клиентских). После — проверено: anon заблокирован везде, authenticated видит лишь свои клиентские RPC, service_role (Edge Functions) работает.
- **`deno check` как гейт**: `npm run check:functions` (7 функций). Исправлены 14 пре-существующих ошибок типов (to-one embed в coach-chat → хелпер `one<T>`, Blob в transcribe, индексы/implicit-any в oura-sync/push-entry-review, `session_note` в workout-import Parsed).
- Мелочь: `remove-imitation-exercise.mjs` больше не считает FK по дропнутой таблице.

### ✅ Клиент — нативный батч (v1.0.1, миграция версии → новый рантайм)
- **Сессия → SecureStore** (`src/lib/secure-storage.ts`): Keystore/Keychain вместо plaintext AsyncStorage; чанкинг (лимит ~2048 байт) + разовая миграция прежней сессии из AsyncStorage (юзер не разлогинивается); web прозрачно падает в AsyncStorage.
- **Нативный датапикер** даты результата в заявке борда (`@react-native-community/datetimepicker`, `maximumDate=сегодня`) вместо ручного «дд.мм.рррр» с парсером.
- **Кнопка «Видалити акаунт»** в Акаунте (двойное подтверждение → `delete_account()` → signOut). Строки en+uk.
- **`expo-blur` НЕ добавлен**: блюр-таббар требует `position:absolute` → контент 6 экранов и инпут коуча прячутся под баром, без устройства не верифицируется — отложено (не слать слепую регрессию). Dep поставлен и снят.
- **Пересборка**: version → 1.0.1 (runtimeVersion=appVersion → рантайм 1.0.1, изолирован от старого APK 1.0.0); `eas build --profile preview` → `3bdebb1a` (IN_PROGRESS на конец сессии).
- Доки: SPEC/USER_FLOWS приведены к реальности (personal_records удалена, метрики в SQL, per-user кап, delete_account).

### 🚧 TODO (деплой/устройство)
- Дождаться билда `3bdebb1a` → APK на S25 → смоук (логин на месте, датапикер, кнопка удаления — НЕ жать на своём акке).

### ⏭️ Следующее
- Хвосты в START HERE (коуч-треды, блюр-таббар с устройством, навбар edge-to-edge).

---

## 2026-07-04 (день-48) — prompt caching + per-user кап + intent в ai_usage; порт грипперов в program-import

Два блока из кандидатов START HERE, оба серверные, один деплой. Верификация: `tsc` ✅ (клиент не задет), `ai-gateway-reviewer` ✅ (блокеров нет), миграция по чек-листу, RPC-смоук новых сигнатур на проде (обе формы вызова ок, смоук-строки подчищены), `month_cost_cap=30` в БД сверен перед деплоем. Живой прогон коуча/импорта не делался — кэш и грипперы подтвердятся первым реальным использованием.

### ✅ Сделано (`5b224df`)
- **Prompt caching (anthropic)**: ephemeral `cache_control` на system + последнем туле + последнем блоке последнего сообщения (макс. 3 брейкпоинта из 4). В агент-цикле коуча каждый следующий вызов читает весь префикс из кэша (×0.1 к цене инпута). `input.messages` НЕ мутируются (коуч переиспользует массив между турнами — иначе брейкпоинты копились бы). Учёт честный: адаптер прокидывает `cache_creation/cache_read`, гейтвей считает кост с множителями 1.25/0.1, а токен-капы видят полный размер промпта.
- **Таймауты**: `AbortSignal.timeout(120s)` во всех трёх адаптерах → `AiError('provider_timeout')` (раньше зависший провайдер держал функцию до её жёсткого лимита).
- **Миграция `20260704090000`**: `ai_usage.intent` (в PK; расход теперь виден в разрезе coach_chat/program_import/transcribe, старые строки → `intent=''`); `ai_limits.user_month_cost_cap=$20` — разогнавшийся юзер блокирует себя, а не весь ИИ обоим (урок инцидента $89M); `ai_budget_check`: день агрегируется sum'ом по интентам, user-кап проверяется до глобального. `ai_record_usage` пересоздана с `p_intent default ''` — вызов `transcribe` без параметра совместим (и обновлён на `p_intent:'transcribe'`).
- **Порт грипперов дня-43 в `program-import`**: структурные `gripper`/`rgc`/`set_type` в JSON-схеме и промпте (вкл. алиасы діп-сета и «RGC → число, не в weight/notes»); копия резолвера workout-import (двунаправленный subset токенов + кириллич. гомоглифы + ближайший RGC, личные первыми); `meta.gripper_id`/фолбэк `gripper_model`+`gripper_rgc`/`set_type` пишутся в `program_sets.meta` — `buildWorkoutFromProgram` уже копирует meta в сеты тренировки как есть; гриппер-упражнение принудительно каноническое «Стиснення еспандера» (`log_kind='gripper'`); вес грип-сета всегда null.
- **Гигиена**: `scripts/tmp-smoke-notify.mjs` → закоммичен как постоянный `scripts/smoke-notify.mjs` (`120f070`).
- Деплой: `db push` + `functions deploy coach-chat|workout-import|program-import|transcribe`.

### ✅ Аналитика → SQL RPC (`b37eb74`, продолжение той же сессии)
- **Миграция `20260704100000`**: `get_analytics_summary()` (security definer, `auth.uid()`) — агрегаты по тренировкам (тоннаж/сеты/RPE/время), топ-5 рекордов вес×повторы и время на упражнение, топ-3 грип-рекордов на установку; вся семантика 1-в-1 со старым клиентским `analyze()`. **Сверка на живых данных Сергея** (юзер-сессия через magiclink + реплика старой агрегации): 16/16 тренировок, 130 сетов, тоннаж 40055.95 до копейки, все топы совпали; ответ RPC 12КБ. + индексы аудита: `workout_exercises(exercise_id)`, partial `sets(logged_at)`.
- **Клиент**: `analytics.ts` → RPC; экран потребляет готовые агрегаты (запросы каталога грипперов и веса тела с экрана убраны — всё в SQL). Персист кэша в AsyncStorage больше не растёт с историей (лимит ~6МБ был реальным риском).
- **Инвалидация (находка `/code-review`)**: `['analytics']` в exercises/index.tsx был мёртвым всегда (не матчил `['analytics-sets']`); ключ сводки теперь `['analytics','summary',userId]`, а финиш/удаление/импорт тренировки инвалидируют `['analytics']` префиксом.
- Ревью: `/code-review medium` (6 углов параллельно) — подтверждена только цепочка инвалидации (починена), остальные кандидаты опровергнуты построчно; `tsc` ✅; `eas update` группа `4422bb86`.

### ⏭️ Следующее
- На устройстве: таб «Аналітика» после апдейта — числа/рекорды/календарь без изменений. Эффект кэша коуча — по `ai_usage.intent`. Грипперы в импорте программы — живым текстом. Остальные кандидаты — в START HERE.
