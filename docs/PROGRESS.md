# Журнал работы / с чего продолжать

> Живой лог по сессиям. Самое свежее — сверху. Подробности фич — в `SPEC.md`.
>
> Архив записей до 2026-06-15 (день-26 и старше, включая весь день старта 06-14): `docs/progress/2026-06-archive.md`.

---

## ▶ START HERE (обновляю в конце каждой сессии)

**Старт сессии:** `git pull --ff-only` → прочитать этот блок → при нужде глянуть свежий раздел ниже и `docs/BACKLOG.md` (секция «Порядок»).

- **Состояние `main` (на 2026-06-23):** бэклог 1/2/6b + §3 ІІ-коуч v1 (см. день-29). День-30б: `workout-import` переработан (натяжка/суперсеты/стороны, структурные грипперы, читинг), per-set тумблер reps⟷сек, тоннаж «обидві» ×2, замена упражнения. **День-31 (НЕ закоммичено, в рабочем дереве):** пачка UI-полишей по фидбеку с устройства — «Утримання»→«Статика»/«Static»; убрано белое мерцание при переходах (`SystemUI.setBackgroundColorAsync`); клавиатура не перекрывает ввод коуча (`behavior=padding` Android + `tabBarHideOnKeyboard`); грипперы в саммари **структурно в 2 строки** (название+RGC / установка·разы·RPE·отдых·рука); **промпт коуча переписан** + фикс пустого ответа (последний турн без тулзов); **аналитика «Сила хвата» — топ-3 на каждую секцию** (дипсет/блоки/карта/TNS), стиль как у обычных рекордов; **главный экран переработан** — resume-баннер активной тренировки, пульс-строка «Цього тижня: N тренувань · тоннаж» (всегда, 0 если не было), карточки с тихим серым номером №N (абс. номер тренировки), датой «21.06.26, Сб» и плюрализацией (вправа/підхід/повтор/тренувань через `src/lib/plural.ts` + namespace `count`), тоннаж отдельной строкой в конце, корзина приглушена opacity.
- **Деплой:** `coach-chat` **передеплоен дважды 06-23** (новая PERSONA + фикс агент-цикла). `workout-import` — версия 06-22. Других ручных действий нет.
- **Данные:** у Сергея **10 чистых тренировок** (см. день-30б), скрипты в `scripts/` одноразовые. Без изменений этой сессии.
- **Следующее:** (1) **закоммитить+запушить день-31** (в git ещё нет, main на других машинах отстаёт; клиентские правки подхватятся релоадом, деплоя не требуют); (2) на устройстве проверить новый тон коуча, плавность переходов, поведение клавиатуры; (3) голос/STT (§3) или §4 IA. Хвосты: 16.06 пустой висяк «One-arm Pull-up» + кисть лево/право; ждёт `sb_secret_…` — `unilateral` на «Боковой нажим», сверка 23.05.

---

## 2026-06-23 (день-31) — UI-полиш по фидбеку с устройства + переписан промпт коуча

Фидбек Сергея со скриншотов/видео с S25. Верификация — `tsc` ✅ (тестов нет), `ai-gateway-reviewer` по коучу — PASS (+~200 токенов/запрос на новую персону, косты-константы не тронуты). Нативные правки (мерцание/клавиатура) в web не воспроизводятся — проверка на устройстве после релоада.

### ✅ Сделано
- **«Утримання» → «Статика» / «Static»** (`summary.holdTime` в обоих локалях). Старое звучало двусмысленно. Если не зайдёт — варианты «Час статики»/«Time under tension».
- **Белое мерцание при переходах убрано** (`src/app/_layout.tsx`). На видео при возврате со саммари к списку вся сцена выбеливалась — это белая «подложка» нативного окна, проступающая в кросс-фейде. Фикс: `SystemUI.setBackgroundColorAsync('#0C0E12')` в рантайме (`expo-system-ui` уже стоял; работает в Expo Go, в отличие от app.json-фона).
- **Клавиатура не перекрывает ввод коуча** (`coach.tsx` + `(tabs)/_layout.tsx`). При edge-to-edge (SDK 54) окно не ресайзится под клавиатуру → `behavior=undefined` на Android оставлял поле под клавиатурой. Поставил `behavior="padding"` для обеих платформ + `tabBarHideOnKeyboard: true` на табе коуча (таб-бар прячется, лишнего отступа нет).
- **Грипперы в саммари переносятся** (`summary/[id].tsx`). Длинная строка эспандера (модель+RGC+установка+повторы+RPE+…) вылазила за край даже на широком экране. Теперь гриппер-подход рендерится вертикально (метка сверху, деталь отдельной строкой во всю ширину); обычные подходы — `flex-1 text-right` с корректным wrap.
- **Промпт коуча переписан** (`coach-chat/index.ts`, PERSONA). Жаловался на «дубовый» вывод — модель выдавала шаблонный SWOT-отчёт «Що добре / Що турбує / Головна рекомендація» с общими советами. Новая персона: явный запрет такого аудита и training-101 банальностей, требование говорить ИХ числами/упражнениями/дисциплиной, живой тон с реакцией и вопросом/следующим шагом в конце. Модель прежняя — Sonnet 4.6 (дело было в промпте, не в модели). Задеплоено.
- **Фикс регрессии агент-цикла** (тот же файл). Новая персона сделала модель прожорливее на тулзы → упиралась в `MAX_TOOL_TURNS=5` всё ещё в режиме tool_use → цикл выходил с пустым `reply` → юзер видел фолбэк «Вибач, не вийшло відповісти». Фикс (бесплатный по костам, число вызовов ≤5 не меняется): на последнем турне инструменты не передаём (`tools: isLast ? undefined : TOOLS`) — модель обязана ответить текстом; + в персону строка «зови тулзы одной параллельной пачкой, не по одной». Передеплоено.

### ✅ Сделано (второй заход дня-31 — ещё фидбек с устройства)
- **Грипперы в саммари — структурно** (`summary/[id].tsx`). Прошлый «перенос во всю ширину» дробил по-дурному (RPE на одной строке, повторы на другой). Теперь гриппер-подход в две строки: стр.1 — модель + RGC, стр.2 — установка · ×повторы · RPE · отдых · рука. Обычные подходы без изменений (`flex-1 text-right`).
- **Аналитика «Сила хвата» — топ-3 по секциям** (`analytics.tsx`). Было: один лучший результат на `set_type`. Стало: `gripBuckets` (Map setType→Map ключ→рекорд, дедуп по эспандер@повторы, ранняя дата), `gripGroups` = топ-3 на секцию, секции сорт по лучшей оценке. Рендер мирроринг обычных рекордов: шапка секции (название установки + ≈лучшая оценка) + нумерованные строки `№. эспандер · RGC · ×повторы → ≈est · дата` через новый `gripLine`.
- **Главный экран переработан** (`(tabs)/workouts.tsx`). (1) **resume-баннер** активной (незавершённой) тренировки наверху — раньше тонул в списке мелким бейджем. (2) **пульс-строка** под заголовком: «Останнє: вчора · Цього тижня: 3 тренування · 6200 кг» (всё из уже загруженного списка, неделя с понедельника). (3) **карточки**: чип с номером `#N` (последняя = total, первая = 1; стабильный индекс, верно пока ≤30 загружено), человеческая дата `Сб, 21.06.26` (`toLocaleDateString` мовой), тоннаж + плюрализованные счётчики. Корзина `graphite-600 → 700` (на тон темнее).
- **Плюрализация** (`src/lib/plural.ts` + namespace `count` в обеих локалях). Hermes/Expo Go не гарантирует `Intl.PluralRules` для uk → категория CLDR (one/few/many) считается вручную; формы строк — в JSON. Покрыто: вправа/підхід/повтор/днів/тренувань.

### 🚧 TODO (деплой)
- `coach-chat` уже задеплоен (06-23). Клиентские правки деплоя не требуют — подхватятся релоадом приложения.
- **Закоммитить+запушить день-31** (в git ещё нет).

### ⏭️ Следующее
- На устройстве: новый тон коуча, плавность переходов, поведение клавиатуры в чате.
- Дальше по бэклогу: голос/STT (§3) или §4 IA-переезд вкладок.

---

## 2026-06-22 (день-30) — выкатка дня-29 на прод + чистка инструментов проекта

### ✅ Сделано
- **Сверена выкатка, разрыв закрыт.** `migration list`: local==remote (все миграции на проде, START HERE про `db push` устарел — поправлен). Edge Functions передеплоены 06-22: `workout-import` v4, `coach-chat` v7, `program-import` v14. Коуч + «Імпорт минулого» теперь живые для обоих юзеров.
- **Skill `progress-log`** добавлен в `.claude/skills/progress-log/` (раньше AGENTS.md на него ссылался, а файла не было).
- **Subagents приведены к реальности.** Из трёх задекларированных в AGENTS.md существовал 0. Решили: не плодить церемонию. Создан один реальный — `.claude/agents/ai-gateway-reviewer.md` (read-only, Sonnet; сторожит бюджет $30/мес, ключи, «метрики считает SQL не ИИ»). `db-migration-reviewer` и `code-reviewer` убраны из AGENTS.md → миграции = инлайн-чек-лист (RLS/деструктив/timestamp), код = встроенный `/code-review`.

### ⏭️ Следующее
- Skill и agent активируются **после рестарта Claude Code** (сканируются на старте).

---

## 2026-06-22 (день-30б) — импорт прошлых тренировок: парсер + грипперы, снос+залив 10 тренировок

### ✅ `workout-import` — переработка парсинга (промпт + код, задеплоено)
- **Натяжка/суперсеты (армрестлинг-нотация Сергея).** Один пункт = одно упражнение со смешанными подходами (динамика+статика); суперсет «кисть⇄отведение» только при явном чередовании, маршрутизация подходов по тегу `(кисть)/(відведення)`; перечисление после стороны = N подходов («права 26, 22с» → 2); «бок»≠«відведення» (anti-rename); статик-строка-заголовок не плодит фантомный подход; «+резинка» = тот же снаряд, деталь в notes; чистка имени от «Поделал». См. память `natyazhka-import-model`.
- **Структурные грипперы** (раньше их `workout-import` не умел). Подход эспандера: вес=null, `meta.gripper_id` (резолв модели по каталогу, **личные грипперы в приоритете**, бренд+цвет различают «300 blue» vs «300 (Gods of Grip)» — оба 72кг), `meta.set_type` (дипсет→deep…), упражнение = каноническое `Стиснення еспандера` (`log_kind=gripper`). Цвет ОБЯЗАН оставаться в модели (различитель).
- **Читинг** (`meta.cheat`) — «с согнутой кисти» → cheat=true. Схема/тип/маппинг подхода расширены.
- Верификация — локальный харнесс `scripts/test-import-prompt.mjs` (9 фикстур, стабильно по 2-3 прогона).

### ✅ Клиент
- **Per-set тумблер reps⟷сек** в карточке подхода (`workout/[id].tsx`) — натяжку можно держать статикой на секундах и динамикой на повторы в одном упражнении (модель/аналитика/программа это уже держали per-row, не хватало affordance). i18n `modeReps/modeTime`.
- **Саммари:** подход с весом без счёта повторов → `14 кг`, а не `14 кг × –`.
- **Тоннаж: «обидві» → ×2** (баг). Аналитика не читала `meta.side`, и «обидві» считалось ×1 — хотя цикл стороны и комментарий в `workout/[id].tsx:445` обещают ×2. Поправлено в 3 местах `analytics.tsx` (тренд/корреляции/календарь): `weight*reps*(side==='both'?2:1)`. Двусторонние упражнения (кисть на лямках, боковой), помеченные «Обидві», теперь дают полный объём.
- **Грипперы по РГЦ в саммари** (`summary/[id].tsx`) — вместо голого «дип-сет ×3» теперь модель+RGC+установка+повторы (`Heavy Grips 300 (blue) · RGC 72 кг · Дип-сет · ×3`). Грузим `listGripperCatalog`. (В аналитике «Сила хвата» RGC уже был.)
- **Замена упражнения вместо тупика удаления** (`exercises/index.tsx` + db). Раньше удаление используемого упр. → нативный белый Alert «не можна видалити», тупик. Теперь: тематический `ConfirmDialog`; если упр. где-то используется — плашка «використовується в N місцях → Обрати заміну» → пикер с поиском (свои+каталог) → `replaceExercise` перецепляет ВСЕ `workout_exercises`/`program_exercises` на выбранное и удаляет старое (подходы/история целы). Db: `countExerciseUsage`, `replaceExercise`. i18n en/uk.

### ✅ Данные (этот заход, service-role)
- «Боковой нажим (бок)» удалён, его подход 21.06 перецеплен на «Бічний натиск» (помечен `both`); «Бічний натиск» → `unilateral=true`.
- Кисть на лямках помечена `both` на всех 4 майских днях (07/12.05 юзер сам, 16/23.05 — скриптом). 16.06 не трогали (там лево/право).

### ✅ Данные (service-role скрипты, `scripts/`)
- `purge-workouts-except.mjs` — снёс 5 кривых тестовых тренировок (оставил 16.06).
- `merge-workout-grippers.mjs` — в 16.06 слил 3 раздробленных эспандера в одно `Стиснення еспандера` (3 подхода, `gripper_id` каждого сохранён).
- `backfill-import.mjs` — залил 9 прошлых тренировок (портирует логику функции; идёт мимо бюджет-гейта, одноразово). 21.06 эспандер вручную перецеплён на личный blue.
- Инструменты: `inspect-workout.mjs`, `list-grippers.mjs` (read-only). Ключи — из `scripts/.openai-key` / env (service-role файл удалён после ротации).

### ⏭️ Следующее / TODO
- **Commit+push** правок дня-30б (код задеплоен/работает локально, но не в git — `main` на других машинах отстаёт). Клиентские правки (тоннаж/гриппер/тумблер) подхватятся релоадом приложения, деплой не нужен.
- **Ждёт secret-ключа** (legacy JWT юзер отключил, нужен `sb_secret_…`):
  - «Боковой нажим (бок)» — кастомное упр. без `unilateral` → нет выбора стороны, нельзя пометить «Обидві». Выставить `unilateral=true` (или юзер сам в «Мої вправи»).
  - Глянуть реальные данные 23.05 в БД (сверить тоннаж после пометки «Обидві»).
- На устройстве пометить двусторонние подходы (кисть на лямках, боковой) как «Обидві» → тоннаж удвоится (правка ×2 уже в коде).
- `ai-gateway-reviewer` на финальный `workout-import` (добавился запрос грипперов) — costs не меняются, по правилу можно прогнать.

---

## 2026-06-19 (день-29) — §3 ІІ-коуч v1: tool-агент над данными атлета

### ✅ Архитектура (согласовано)
- **SQL + tool-calling агент, БЕЗ векторки.** Ценность коуча — приватный точный доступ к реальным структурным данным атлета через инструменты (а не семантический поиск). Векторка тащит инфру и почти ничего не даёт на числовых тренировочных данных; pgvector включим потом, если появится текстовый корпус. «Умнее ChatGPT» = приватные свежие данные через тулзы + структурная память + честность по свежести.

### ✅ Сделано (полный вертикальный срез)
- **Гейтвей: tool-use.** `_shared/ai/types.ts` — структурный `content` (text/tool_use/tool_result) + `ToolSpec` + `CompleteOutput.toolUses/stopReason`. `anthropic.ts` — прокидывает `tools`, парсит `tool_use`-блоки. `gateway.ts` `RunResult` несёт `toolUses/stopReason`. Обратносовместимо (program-import на openai, шлёт строки).
- **Edge `coach-chat`** — агент-цикл (до 5 итераций, бюджет через `runIntent` на каждый вызов). Инструменты (read-only, скоуп по userId): `get_profile`, `get_recent_workouts`, `get_exercise_history`, `get_records`, `get_recovery` (с датой/возрастом — честно про стейл OURA), `get_cycle` (фаза по `profile.track_cycle`), `get_facts`, `remember_fact` (пишет в `coach_facts`). Persona: поддерживающий тренер, язык/единицы юзера, цикл только если трекается, не выдавать стейл за «сегодня».
- **Память:** миграция `20260619120000_coach_facts` (kind/content/RLS). Чат — переиспользует `ai_threads`/`ai_messages`.
- **Клиент:** `src/lib/db/coach.ts` (listCoachMessages/sendCoachMessage). Экран `src/app/(tabs)/coach.tsx` — пузыри, ввод, пустое состояние с подсказками, индикатор «думаю». Таб «Коуч» (🧠) в `_layout`. i18n en/uk.
- Проверка: `tsc` ✅, JSON ✅, web-бандл собирается без ошибок. Живой ответ агента — после деплоя (см. START HERE).

### 🚧 TODO (деплой) — ты
- [ ] `npx supabase db push` (миграция `20260619120000_coach_facts`).
- [ ] `npx supabase functions deploy coach-chat`.

---

## 2026-06-18 (день-28б) — «single»-блок больше не кластер + доводка стороны

### ✅ Сделано (клиент)
- **Корень бага «программа→тренировка».** Импортёр складывал несколько самостоятельных упражнений в один блок `type=single` (лейбл «single»/«Турник»/«Эспандер»). Приложение считало кластером **любой** блок с >1 упражнения → рисовало бессмысленную шапку и раунд-за-раундом схлопывало первые подходы. Теперь блок — кластер **только если type — настоящая группировка** (суперсет/коло/EMOM…), `single` — никогда. Single-блок рендерится отдельными карточками без шапки. Общие хелперы `isClusterBlock` (programs.ts) / `isClusteredWorkoutExercise` (workouts.ts) вместо дублей. Применено в: программа, старт тренировки (`block_key=null`), экран тренировки (+ existing тренировки со старым `block_type='single'`), саммари. Бонус: русские лейблы блоков ушли сами (у single шапки нет).
- **Проверено на вебе:** «Upper strength» — Жим/Кисть/Швунг отдельными карточками; старт → все 5 жимов по отдельности, не схлопнуты. «Grip training» — грипперы/підтягування плоско, без «Турник»/«Эспандер».
- **«Обидві» убрано из стороны.** Сторона показывается только у односторонних/хвата, где «обидві» противоречива → цикл `нет → Ліва → Права → нет`. Тип `SetSide`/перевод `side_both` оставлены для показа старых данных. Проверено на вебе (one-arm pull-up): `Сторона → Ліва → Права → Сторона`.
- **Данные:** почистил 1 исторический подход с `side=both` на «Підтягування на одній руці» (мис-тап из «5 отличий») → meta=null. Вес 95кг не трогал.

### ✅ Подтверждено по БД
- Миграция `20260617120000` (флаг `unilateral`) **уже прокатана**: колонка живая, 30 упражнений `unilateral=true`. «Сторона» работает по флагу, не по эвристике. TODO деплоя закрыт.

### ✅ Дубль-упражнения → подходы (фидбек: «3 стиснення еспандера вместо 3 подходов, не могу добавить подход»)
- **Корень:** импортёр дробил один снаряд на разных грипперах (эспандер ×5/×2/×4) на **3 отдельных `program_exercise`** с одинаковым `exercise_id` по 1 подходу → в тренировке 3 свёрнутых карточки, «Додати підхід» негде нажать.
- **Данные:** `scripts/merge-dup-exercises.mjs` слил подряд идущие PE с одинаковым `exercise_id` в одно упражнение с N подходами (по всем программам юзера; затронуло только Grip training: 3 Gripper Close → 1 упр × (5,2,4)). Сухой прогон → потом `--apply`. Проверено на вебе: программа показывает одно «Gripper Close» с 3 подходами.
- **Импортёр (`program-import`):** теперь если упражнение в блоке повторяет `exercise_id` предыдущего — дописывает его подходы в то же упражнение, а не плодит новые PE. **Нужен `supabase functions deploy program-import`.**

### ✅ Импорт: чиним ПРИЧИНУ (фидбек: «делай корректный импорт, чтобы не терять инфу»)
- **Урок:** ретро-слияние дублей было неверным — три «Gripper Close» = три РАЗНЫХ гриппера, при слиянии их per-set описания (модели эспандеров) потерялись. Правильное место правки — сам импорт, не постфактум-чистка.
- **Промпт `program-import` усилен** (структура на выходе ложится в модель без потерь):
  - «ONE MOVEMENT = ONE EXERCISE WITH MANY SETS» — один и тот же снаряд/движение никогда не дробится на несколько «упражнений»; повторные рабочие подходы = подходы одного упражнения. Per-set отличия (нагрузка, модель эспандера, резина, темп, сторона) живут НА ПОДХОДЕ (вес/`notes`), не как новое упражнение и не теряются.
  - **Грипперы:** нагрузка = модель гриппера, не вес → одно упражнение «Стиснення еспандера», по одному подходу на гриппер, weight=null, модель в `notes` подхода. Три гриппера ×5/×2/×4 → одно упр, три подхода, три заметки. (program view уже показывает `s.notes` в строке подхода.)
  - «single»-блок = строго одно упражнение (чтобы не валить несколько движений в один блок, как было в Upper strength).
- **Код-страховка:** при слиянии дубль-упражнения его описание переносится на подход (`setFields(s, ex.notes)`), чтобы инфо не пропадала, даже если модель снова положит её в notes упражнения.
- Гейтвей умеет только `json:true` (строгий JSON), без json-schema — добавлять structured output потребовало бы переписать 3 адаптера; промпт-фикс решает задачу. **Нужен `supabase functions deploy program-import`.**

### 🔧 Окружение
- Дев-сервер на 8081 занял `EUSAProcess.exe` → превью посадил на 8090 (`.claude/launch.json`, локально, не в коммит).

---

## 2026-06-18 (день-28) — фиксы логики по фидбеку (таймер, 1ПМ сингла, грипперы, фазы)

### ✅ Сделано (клиент)
- **Таймер 27 годин — фикс.** `finishWorkout` ставил `ended_at=зараз` при каждом «Завершити» → редактирование вчерашней раздувало длительность (старт вчора, кінець сьогодні). Теперь `ended_at` пишется только если ещё null (`.is('ended_at', null)`). + разово починил данные: у тренировки Сергея `ended_at = старт + разброс logged_at` → 11 хв (был косяк: при переносе на вчера двигал workout, но не logged_at подходов).
- **1ПМ для сингла.** `oneRmEst`: для `reps≤1` оценка = сам вес (раньше 95×1.025≈97 на 1 раз — бред). 
- **Грипперы в рекордах (RGC-ПР, раздельно по установке).** Гриппер-подходы (вес=null) вылетали из рекордов. Теперь секция **«Сила хвата»**: оценка 1ПМ = `RGC×(1+0.025×повт)` (для сингла = RGC), **лучший по каждому виду установки** (TNS/карта/блок-38/блок-20/дип-сет — отдельные ПР, т.к. карта≠дип-сет). `analyze` берёт RGC из `listGripperCatalog` (gripper_id→rgcInKg). Грипперы больше не засоряют кластерные рекорды.
- **Саммари: гриппер-строка** — показывает установку + повторы (`З карти · × 5`), а не «– кг × 5».
- **Календарь: фазы цикла только при `trackCycle`.** Не-женский профиль → нет ни тонировки фаз, ни легенды, ни фазы в детали дня.
- Проверка: `tsc` ✅, JSON ✅.

### ✅ Доводка по фидбеку (скрины саммари/хват)
- **Пустые упражнения убраны из саммари** — упражнение без единого отмеченного подхода (пустой «висяк» от кривого импорта) больше не рисуется как «не виконано».
- **RGC в рекордах хвата** — строка показывает, от чего считается оценка: `Heavy Grips 300 · RGC 72 кг · × 4 → ≈79.2 кг`.
- Гейт «нет грипперов → нет секции «Сила хвата»» — уже работал (`gripRecords.length > 0`).

### ⏭️ На решение пользователя (его данные, не баги)
- Лейблы блоков по-русски («Эспандер», «Кисть с ручкой на лямке») — текст из программы; нет UI для правки лейбла блока (можно поправить разово в БД или дать редактирование).
- «Обидві» как сторона у одностороннего упражнения — предложено убрать «обидві» из вариантов для `unilateral`.

---

## 2026-06-17 (день-27г) — фикс таймера при редактировании + управление упражнениями в кластере

### ✅ Сделано (клиент)
- **Таймер при «Редагувати»** (фидбек): переоткрытая завершённая тренировка показывала «Час: 159:33» и бешеный «Відпочинок» (считалось `now − started_at`). Теперь при `ended_at`: «Час» заморожен на реальной длительности (`ended_at − started_at`, без тикания), «Відпочинок» скрыт.
- **Удаление/перестановка упражнения внутри кластера** (фидбек: висяк «One-arm Pull-up» от кривого импорта — убрать одно, не весь блок). В развёрнутом кластере (если >1 упражнения и блок не завершён) — строка «Вправи блоку»: у каждого упражнения ↑/↓ (reorder, `reorderWorkoutExercises` — переиспользуем существующие слоты order_index, т.к. он глобальный) и ✕ (убрать одно через тематический confirm + каскад). Это и закрывает **reorder в тренировке** (скрин 3, ч.2).
- Данные: реальную тренировку Сергея (аккаунт `a80ddd8a`, не Маши!) перенёс на вчера (16.06) разовым service-role скриптом.
- Проверка: `tsc` ✅, JSON ✅.

### ✅ Имена упражнений — флип на локализованные (реверс дня-11)
- Для **сматченных** упражнений (exercise_id) показываем имя из каталога через `exerciseName(ex, lang)` (следует uk/en); кастомные/несматченные — `display_name` (как в программе). Применено в тренировке (`exName`), саммари, аналитике. `tsc` ✅.
- Блок-лейблы (`ТУРНИК`) и имена кастомных — текст пользователя/импорта, правятся в программе / «Мої вправи».

### ✅ Ревизия каталога — применена (правки из catalog-review.xlsx)
- Выгрузил каталог в `catalog-review.xlsx` (exceljs; Python на машине — App Store-заглушка, не настоящий). Пользователь разметил 39 правок в колонке «Коментар / правки».
- **Миграция `20260617120000_catalog_revision.sql`** (нужен `db push`):
  - **`exercises.unilateral boolean`** — настоящий флаг «есть сторона» вместо эвристики. `unilateral=true` для 30 упражнений (эвристика grip/one-arm/wrist + 20 явных «зробити двохстороннім»: випади, болгарський, тяга гантелі, біцепс, всё гирьове, армрестлінг-односторонні, бічна планка…).
  - **15 переименований/переводов:** Віс→Вис, Негативні підтягування, Млин з гирею (Kettlebell Windmill), Оберти млинця, розгинання тріцепса, One-arm Pull-up→Підтягування на одній руці, + кастомные lowercase-фрагменты из импорта (Негативні відтискання, Розводка резинки/гантелей, Утримання над головою…) — чистое имя + EN, без веса.
  - **Удаление мусора** (guarded by FK): Імітація руху, Російські скручування, Пир, По.
- **Клиент:** `exerciseSided(ex)` в `exercises.ts` (флаг `unilateral` → иначе эвристика), переиспользован в воркауте (убрал дубль). `Exercise.unilateral` в типе. До `db push` — деградирует на эвристику.
- **Авто-капитализация:** `createCustomExercise` поднимает первую букву (ввёл с маленькой → станет с большой) + ставит `unilateral` по эвристике имени.
- Проверка: `tsc` ✅.

### 🚧 TODO (деплой) — ты
- [ ] `npx supabase db push` (миграция `20260617120000` — `unilateral` + ревизия). До этого «Сторона» работает по эвристике.
- Заметка: «Російські скручування» — если используется в тренировке, удаление пропустится (FK-гард); можно завести заново нейтральным именем «Скручування з поворотом».

---

## 2026-06-17 (день-27в) — лок завершённого блока + сторона только где нужна

### ✅ Сделано (клиент)
- **Завершённый блок/упражнение — только чтение** (фидбек, скрин 1). При `allDone`/`done_at`: прячем «Додати раунд»/«Додати підхід», у подходов блокируем поля (вес/повт/RPE), ✕-удаление, ✓ и сторона/читинг — `SetRow locked`. Чтобы править — сначала «Відновити блок/вправу».
- **Сторона только для односторонних/хвата** (скрин 2). `SetRow sided` через `isSided`: показываем выбор стороны для one-arm/однією рукою/на одній нозі/wrist/кисть и для grip; для двусторонних (підтягування, жими, тяги штанги, становая, запрыгивания) — скрыто. Читинг остаётся у всех.
- Проверка: `tsc` ✅.

- **Редактировать завершённую тренировку** (скрин 4): кнопка «Редагувати» на саммари → открывает ту же тренировку в редакторе (`/workout/[id]`). Переоткрытая = вся залочена (done), тапаешь «Відновити» на нужном → правишь → «Завершити» → назад на саммари. Минорный хвост: таймер «Час» покажет время от старта (косметика).

### ✅ Reorder в редакторе программы (скрин 3, ч.1) — ↑/↓
- Согласовали: **↑/↓-кнопки** (не drag), оба экрана. Сделан **редактор программы**: в режиме «Редагувати» у упражнения кнопки ↑/↓ (когда есть с кем меняться). Соседи берутся по `block_id` → переставляются и упражнения **внутри блока**, и **standalone** между собой. `reorderProgramExercises` перенумеровывает 0..n-1 (устойчиво к дублям order_index из импорта). Закрывает кейс «one-arm pull-up влепило не туда». `tsc` ✅.
- **Хвост:** singles всегда после блоков (отдельные пространства order_index) — single выше блока пока не подвинуть. И **reorder в самой тренировке** (кластер рисуется по раундам — нужен отдельный контрол на уровне упражнения) — следующий заход.

---

## 2026-06-17 (день-27б) — читинг + сторона у підході (per-set meta)

### ✅ Сделано (клиент, деплой/миграция не нужны — пишем в существующий `sets.meta`)
- **Читинг по подходу** — чекбокс «Читинг» в строке подхода (`SetRow`), янтарный когда вкл. Кейс: «кисть ручкою на лямці» можно строго или «слегка заряжено» — теперь отмечается у конкретного подхода.
- **Сторона (ліва/права/обидві)** — компактная пилюля, циклится тапом: нет → Ліва → Права → Обидві → нет. Без новых окон.
- Хранение: расширил `GripMeta` (→ общий meta подхода) полями `cheat?`/`side?` + тип `SetSide`. `save` теперь пишет `meta` для любых упражнений (не только grip). Метки видны в **саммари** (строка подхода). i18n en/uk.
- Проверка: `tsc` ✅, JSON ✅.
- **Рекорди — сноска про читинг:** в тоннаж/статистику читинг идёт **как обычно** (решение пользователя), а в дереве рекордов запись с читингом помечается янтарной сноской «· Читинг» (`getLoggedSets` тянет `meta` → `analyze` несёт `cheat` → `recLine`). Не исключаем, просто помечаем.

---

## 2026-06-17 (день-27) — OURA-аналітика, заход 1: секція «Відновлення» (середні + спарклайни)

### ✅ Сделано (клиент, деплой не нужен)
- **Новая секция «Відновлення»** в Аналітиці (`analytics.tsx`) — первый слой §6b BACKLOG. По каждой метрике OURA (готовність, оцінка сну, HRV, RHR, тривалість, ефективність, дихання, темп., SpO₂, стрес, кроки): **последнее значення + середнє за 30 дн + мини-спарклайн + дельта (%) к попередніх 30 дн**. Дельта окрашена по «направлению» метрики (↑/↓ better → accent/red, нейтральные — серым).
- Данные: `getSnapshotsRange(userId, today−59)` (берём 60 дн, чтобы посчитать текущий период и предыдущий для дельты). `analyzeRecovery` — чистая функция; карточка рисуется только если у метрики ≥3 точек и есть данные за последние 30 дн.
- Секция показывается **независимо** от наличия тренировок (есть OURA-данные → видна); пустой плейсхолдер — только когда пусто и тренировки, и recovery. Лейблы/единицы переиспользованы из `health.metrics.*`/`health.units.*` (без дублей). Новые i18n: `analytics.recovery/recoveryHint/avg30` (en/uk).
- Проверка: `tsc` ✅, JSON ✅. Живой прогон в браузере не делал — секция требует реальных OURA-снимков аккаунта Маши (нет кредов/данных на этой машине).

### ✅ Доработки по фидбеку (тот же день)
- **Проценты починены.** Дельта (%) теперь показывается только у метрик с понятным «лучше/хуже» (dir≠0). Температура/дыхание — база ~0, процент давал мусор («134%») → у них только спарклайн+тренд, без %. (Непоследовательность «где-то есть %, где-то нет» была из-за наличия/отсутствия данных за предыдущий период.)
- **Выбор периода** в «Відновлення»: пилюли **30 / 90 / 180 дн**. Тянем до 365 дн истории (`getSnapshotsRange(today−365)`), окна и дельта считаются по выбранному периоду; спарклайн даунсемлится в ~28 бакетов. Причина: для цикла 30 дн = выборка из одного эпизода фазы, мало (фидбек пользователя). **NB:** OURA-история сейчас ~31 день, так что 90/180 наполнятся со временем.
- **Убрано автообновление** OURA при заходе на Здоров'я (`useFocusEffect` удалён) — синк только по кнопке «Оновити».

### ✅ Глубокая история + фикс кэша (тот же день)
- **«Оновити» тянет 365 дн**, а не 30 (`syncOura(365)`): OURA-облако хранит всю историю, апсёрт не удаляет → один тап наполняет БД на год. Раньше тянули 30 → в БД лежал ровно месяц, периоды 90/180 упирались в него. После правки у Маши **записано 204 дня**.
- **Фикс кэша:** синк на Здоров'ї теперь инвалидирует и `analytics-recovery` (не только `oura-snapshot`) — иначе графики аналитики держали старое окно и 90/180 выглядели одинаково даже при наличии истории. (Урок: довести взаимосвязанную цепочку data→consumers→caches до конца за один проход.)

### ✅ Кореляції «тренування × відновлення × цикл» (§6b, заход 2)
- Новая секция **«Тренування × Відновлення»** в Аналітиці: сшиваем по дате тренировки (`getLoggedSets` + снимки OURA + фаза цикла).
  - **Готовність/Сон у дні тренувань** vs средние за период (тренируется ли Маша на восстановленных днях).
  - **Тренування по фазах циклу** — счётчик по фазам (там, где фаза известна).
  - **Лента «по календарю»** (до 15 тренировок): дата · фаза · готовність · сон · RPE · тоннаж в одной строке.
- `getLoggedSets` теперь тянет и **`rpe`** (сигнал «лёгкости»). `db/cycle.ts`: `getPeriodStarts` + `phaseForDate(ymd, starts)` — фаза на историческую дату по отметкам «день 1»; если отметок нет/день>40 → фаза неизвестна (не выдумываем). Секция и периоды слушают те же пилюли 30/90/180.
- Whole-chain: отметка нового цикла инвалидирует `analytics-cycle-starts`. Проверка: `tsc` ✅, JSON ✅.

### 🩸 OURA — решение: PAT → OAuth (см. BACKLOG §6c)
Диагностика Маши: HTTP 200 везде, но ночные данные застряли на 06-15 (активность 06-17) при ежедневном синке кольца. Застой в 2 дня — ограничение PAT, не лаг облака. **Решено: переезжаем на OAuth.** Пока кольца рядом нет — по OURA ничего не трогаем.

### ✅ Календарик + відмітка «день 1» + мок-дані (§6b — закрито)
- **Календар** в Аналітиці: месячная сетка (‹ ›-навигация, не дальше текущего месяца), по дню — тренировка (акцентная точка), **фаза цикла как фоновый тон** (менструальна/фолікулярна/овуляція/лютеїнова), отметки «день 1» обведены. Тап по дню → лист: дата (повна ДД.ММ.РРРР), фаза, готовність/сон, тренування+тоннаж, и кнопка **«Позначити день 1 циклу»** (для відстеження циклу) → `logPeriodStart(userId, day)`. Так Маша размечает исторические старты прямо в календаре, **фазы считаются автоматически** (`phaseForDate`) и сразу подтягиваются в корреляции (whole-chain: инвалидируем `analytics-cycle-starts` + `cycle`).
- **Дата в ленте** — полная `ДД.ММ.РРРР` (раньше «15.6» читалось как «параграф»).
- **Мок-данные для превью:** `scripts/seed-mock-workouts.mjs` (service role, тег `notes='mock-seed'`, `--clean`). Засидил 10 тренувань × 8 підходів за 19.05–15.06 на аккаунт с OURA → проверил: 10/10 попадают на дни со снимками; превью карточек Готовність 82 (сер.83) / Сон 80 (сер.81). Реальные тестовые тренировки пользователь удалил вручную, оставил мок.

### ⏭️ Следующее
- Снять мок (`--clean`), когда визуал утверждён.
- Возможно: un-mark «день 1» из календаря (сейчас разметка только ставится; снять — через id, есть `deletePeriod`).
- IA-переезд вкладок / ІІ-коуч (BACKLOG §1, §3).

### ⏭️ Следующее (§6b, по слоям)
- **Корреляции «тренування ↔ відновлення ↔ цикл»** (главная ценность): наложить RPE/тоннаж/выполнение плана на readiness/сон/фазу цикла по `date` (`getLoggedSets` + снимки + `cyclePhase`).
- Дальше — **календарик** по дням.

---
