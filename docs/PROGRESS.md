# Журнал работы / с чего продолжать

> Живой лог по сессиям. Самое свежее — сверху. Подробности фич — в `SPEC.md`.

---

## 2026-06-15

### ✅ Сделано
- **Реактивная смена kg/lb** — единица в общем сторе (`useSyncExternalStore`, `src/lib/use-unit.ts`); смена в Акаунте обновляет экраны мгновенно. Инициализация на старте (`_layout`), обновление из профиля (гейт), онбординга и Акаунта через `setWeightUnit`/`applyUnit`.
- Подтянул работу с дома (табы / RPE / таксономия / каталог 82); проверил: tsc + web-бандл зелёные, миграции в облаке применены.
- **OURA через PAT — заложено (ждёт деплоя + токена).** Решили PAT, не полный OAuth (для 2 юзеров проще).
  - Миграция `20260615120000_oura_pat_and_cycle.sql`: PAT-режим `private.oura_tokens` (refresh/expiry nullable), флаг `profile.oura_connected`, поля `health_snapshots.cycle_day/cycle_phase`, таблица `cycle_periods`, security-definer RPC `store_oura_token`/`get_oura_token` (схема private не экспонирована).
  - Edge Functions: `oura-connect` (валидирует токен у OURA + хранит), `oura-sync` (тянет daily_readiness/daily_sleep → `health_snapshots`).
  - UI вкладки Здоров'я: вставка PAT → подключение → Readiness/Sleep + «Оновити». Слой данных `src/lib/db/oura.ts`.

### 🚧 TODO по OURA (деплой)
- [ ] `npx supabase db push` (применить миграцию)
- [ ] `npx supabase functions deploy oura-connect oura-sync`
- [ ] Позже: Маша генерит OURA Personal Access Token (cloud.ouraring.com) и вставляет во вкладке Здоров'я; отладка с реальным кольцом.
- [ ] HRV/RHR пока не маппятся (только readiness/sleep score + raw) — уточнить поля на реальных данных. Цикл: схема готова, расчёт фазы + сид дат Маши — позже.

### 🔜 Дальше (порядок согласован: 4 → 2 → 1)
1. ~~kg/lb вживую~~ ✅ · ~~OURA заложить~~ ✅ (ждёт деплоя)
2. **Программы + ИИ-импорт** (§3.7) — следующее крупное.
3. Аналитика (фаза 2).

---

## 2026-06-14

### ✅ Сделано
1. **Каталог упражнений + таксономия** (#3)
   - Миграции: `supabase/migrations/20260614120000_exercise_taxonomy.sql` (поля `cluster`/`category` + констрейнты) и `20260614120100_seed_full_catalog.sql` (~82 упражнения, двуязычные, собраны из дневника Маши).
   - **Применены к облаку через `supabase db push`** (сделал пользователь).
   - Пикер упражнений: группировка по кластерам (Верх/Низ/Все тіло/Кор), категория в строке, поиск по названию+алиасам. Файлы: `src/lib/db/exercises.ts`, `src/app/workout/[id].tsx`.
2. **RPE выпадающим выбором** — зоны 🟢1–4 / 🟡5–7 / 🔴8–10 с подсказками вместо ручного ввода. `src/app/workout/[id].tsx` (`RpePicker`).
3. **UX-фиксы пикера:** клавиатура не открывается сама, секция «Часті» сверху (`getRecentExercises`), сворачивание клавиатуры свайпом, сброс строки поиска при открытии.
4. **Нижние табы** (`src/app/(tabs)/`): Тренування / Програми / Аналітика / Здоров'я / Акаунт.
   - Sign out переехал в **Акаунт** (там же язык + единицы).
   - OURA — карточкой во вкладке **Здоров'я** (опционально). Програми/Аналітика — заглушки.
5. **Фикс бага с языком:** язык восстанавливается на старте приложения (`src/app/_layout.tsx` + `src/lib/prefs.ts`), больше не сбрасывается на `en` после релоада.
6. **SPEC обновлён до v0.4** — таксономия/RPE (реализовано), импорт программ ИИ (§3.7) и цикл в OURA (§3.5) зафиксированы как план.

### 🔧 Открытые хвосты (с чего начать завтра)
- [ ] **Телефон не видит изменений, а web (localhost) видит.** Скорее всего Expo Go/dev-клиент держит старый бандл. Пробовать: `npx expo start -c` (чистый кэш), перетряхнуть/Reload на телефоне, проверить что телефон и ПК в одной сети.
- [ ] **Смена единиц kg/lb в Акаунте не «вживую»** — `useWeightUnit()` читает AsyncStorage только на маунте. Сделать реактивным (контекст/стор), чтобы экран тренировки обновлялся сразу.
- [ ] Проверить табы на устройстве: иконки-эмодзи, отступы, что workout/summary открываются поверх табов.

### 🔜 Следующие большие фичи (по приоритету)
1. **#1 Программы + импорт через ИИ** (§3.7): таблицы `programs/program_exercises/program_sets`, экран «Програми», Edge Function парсинга текста/файла → упражнения (файлы не храним).
2. **#2 OURA + цикл** (§3.5): OAuth2 Edge Function, `health_snapshots` + `cycle_day/cycle_phase`, подтяжка readiness/сна; засидить даты цикла Маши (лист «КД»).
3. **Аналитика** (фаза 2): SQL-вьюхи/RPC (тоннаж, объём, 1ПМ, PR) + графики.

### Зацепки
- Бэк решили оставить на **Supabase (SQL) + Edge Functions на TypeScript** — Python не вводим (см. обсуждение; оправдан только под будущий data-science отдельным воркером).
- Данные Маши (упражнения, рекорды, даты цикла) — в её Google-таблице, листы «Тренування2025», «Трені 2026», «Рекорди», «КД».
