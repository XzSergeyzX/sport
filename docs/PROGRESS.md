# Журнал работы / с чего продолжать

> Живой лог по сессиям. Самое свежее — сверху. Подробности фич — в `SPEC.md`.
>
> Архивы: `docs/progress/2026-07-archive.md` (день-31…день-38), `docs/progress/2026-06-archive.md` (день-30 и старше).

---

## ▶ START HERE (обновляю в конце каждой сессии)

**Старт сессии:** `git pull --ff-only` → прочитать этот блок → при нужде глянуть свежий раздел ниже и `docs/BACKLOG.md` (секция «Порядок»).

- **Состояние `main` (на 2026-07-04, `1168afd` + доки):** день-48/49 закрыли почти все хвосты аудита дня-46. Серверное (всё в проде, миграции по `20260704120000`, все 7 функций задеплоены): prompt caching + per-user кап $20 + `intent` в `ai_usage` + таймауты; аналитика→SQL RPC `get_analytics_summary`; **`personal_records` удалена** (рекорды коуча/аналитики из `sets` одной функцией `analytics_summary_for`); **`delete_account()` RPC** (Play Store); **security-фикс: закрыта дыра в грантах definer-функций** — `revoke from anon,authenticated` НЕ снимал грант `PUBLIC`, anon по публичному ключу мог дёргать `get_oura_token`(чужой OURA-токен!)/`analytics_summary_for`(чужие тренировки)/`ai_record_usage`(накрутить чужой кап); теперь `revoke from public` + точечные гранты (проверено на проде). `deno check` заведён как `npm run check:functions` (7 функций зелёные). **Клиент (v1.0.1, требует НОВЫЙ APK):** сессия→SecureStore, нативный датапикер даты результата, кнопка удаления аккаунта.
- **⏳ Твои действия:** **дождаться EAS-билда `3bdebb1a`** (preview APK v1.0.1) → установить на S25 поверх старого. Проверить: (1) логин НЕ слетел (миграция сессии AsyncStorage→SecureStore прозрачная), (2) в заявке борда «Дата результату» — нативный календарь, (3) Акаунт → «Видалити акаунт» есть, но **НЕ жать на своём акке** (снесёт данные Сергея каскадом — это для тест-акка). JS-канал для 1.0.1 — тот же `eas update --channel preview`, но публиковать ТОЛЬКО из текущего репо (рантайм 1.0.1); старый APK остаётся на 1.0.0.
- **Данные:** у Сергея реальные тренировки (№1–15) — при тестах НЕ трогать. Роли на проде: Сергей=admin (S25), Маша=full (**iPhone** — TestFlight в этапе 3 §9). Антиспам 5 заявок/24ч на admin не действует (день-47). `scripts/smoke-notify.mjs` — смоук пушей.
- **Следующее:** **Apple/TestFlight (этап 3 §9) — ТОЛЬКО по явной команде Сергея.** Остаток: коуч треды/«Нова розмова» + дата-разделители (день-31); **блюр-таббар отложен** (требует `position:absolute` → контент 6 экранов и инпут коуча прячутся под баром; без устройства не верифицируется — делать с девайсом); навбар edge-to-edge проверить (день-44). Долг: формула тоннажа в 3 местах (RPC аналитики / вью `workout_summaries` / клиентский `workoutStats`) — при изменении правил править все три.

---

## 2026-07-04 (день-49) — добить хвосты аудита + security-фикс грантов + пересборка APK v1.0.1

Продолжение дня-48 («добиваем все хвосты и пересборку»). По ходу работы над `delete_account` **всплыла серьёзная пре-существующая уязвимость** и была закрыта. Верификация: `tsc` ✅, `deno check` (новый `npm run check:functions`, все 7 функций) ✅, `ai-gateway-reviewer` — блокеров нет, живые смоуки на проде (coach `get_records`, `delete_account` на одноразовом юзере, грант-локдаун по трём ролям).

### ✅ Серверные хвосты (миграции `20260704110000`, `20260704120000`; функции задеплоены)
- **`personal_records` ВЫКИНУТА** (пуста, никто не писал): рекорды коуча теперь из `sets` через `analytics_summary_for(p_user)` — та же функция, что питает экран аналитики (`get_analytics_summary` — тонкая обёртка над ней по `auth.uid()`). Живой прогон коуча подтвердил: «топи» + грип-настройки (deep/CoC) озвучиваются верно.
- **`delete_account()` RPC** (Play Store, этап 4 §9): security definer, удаляет строго `auth.uid()` из `auth.users` → каскад по всем таблицам; `leaderboard_entries.verified_by` переведён на `on delete set null` (апрувы переживают уход модератора). Смоук на одноразовом юзере: каскад чистый.
- **🔴 SECURITY: закрыта дыра в грантах.** `revoke execute … from anon, authenticated` НЕ снимает дефолтный грант роли `PUBLIC` (её члены — и anon, и authenticated). Итог: ВСЕ security-definer функции были вызываемы анонимно по публичному anon-ключу. Проверено на проде — anon мог: `get_oura_token(любой uuid)` → **вытащить чужой OURA access-token** (прямое нарушение «ключи только на сервере»), `store_oura_token` → перезаписать, `analytics_summary_for/get_analytics_summary` → **читать чужие тренировки**, `ai_budget_check/ai_record_usage` → читать/**накручивать чужой ИИ-бюджет** (упереть юзера в кап). Фикс (миграция `120000`): `revoke … from public, anon, authenticated` → явный `grant` только нужной роли (service_role для серверных, authenticated для клиентских). После — проверено: anon заблокирован везде, authenticated видит лишь свои клиентские RPC, service_role (Edge Functions) работает.
- **`deno check` как гейт**: `npm run check:functions` (7 функций). Исправлены 14 пре-существующих ошибок типов (to-one embed в coach-chat → хелпер `one<T>`, Blob в transcribe, индексы/implicit-any в oura-sync/push-entry-review, `session_note` в workout-import Parsed).
- Мелочь: `remove-imitation-exercise.mjs` больше не считает FK по дропнутой таблице.

### ✅ Клиент — нативный батч (v1.0.1, миграция версии → новый рантайм)
- **Сессия → SecureStore** (`src/lib/secure-storage.ts`): Keystore/Keychain вместо plaintext AsyncStorage; чанкинг (лимит ~2048 байт) + разовая миграция прежней сессии из AsyncStorage (юзер не разлогинивается); web прозрачно падает в AsyncStorage.
- **Нативный датапикер** даты результата в заявке борда (`@react-native-community/datetimepicker`, `maximumDate=сегодня`) вместо ручного «дд.мм.рррр» с парсером.
- **Кнопка «Видалити акаунт»** в Акаунте (двойное подтверждение → `delete_account()` → signOut). Строки en+uk.
- **`expo-blur` НЕ добавлен**: блюр-таббар требует `position:absolute` → контент 6 экранов и инпут коуча прячутся под баром, без устройства не верифицируется — отложено (не слать слепую регрессию). Dep поставлен и снят.
- **Пересборка**: version → 1.0.1 (runtimeVersion=appVersion → рантайм 1.0.1, изолирован от старого APK 1.0.0); `eas build --profile preview` → `3bdebb1a` (IN_PROGRESS на конец сессии).
- Доки: SPEC/USER_FLOWS приведены к реальности (personal_records удалена, метрики в SQL, per-user кап, delete_account).

### 🚧 TODO (деплой/устройство)
- Дождаться билда `3bdebb1a` → APK на S25 → смоук (логин на месте, датапикер, кнопка удаления — НЕ жать на своём акке).

### ⏭️ Следующее
- Хвосты в START HERE (коуч-треды, блюр-таббар с устройством, навбар edge-to-edge).

---

## 2026-07-04 (день-48) — prompt caching + per-user кап + intent в ai_usage; порт грипперов в program-import

Два блока из кандидатов START HERE, оба серверные, один деплой. Верификация: `tsc` ✅ (клиент не задет), `ai-gateway-reviewer` ✅ (блокеров нет), миграция по чек-листу, RPC-смоук новых сигнатур на проде (обе формы вызова ок, смоук-строки подчищены), `month_cost_cap=30` в БД сверен перед деплоем. Живой прогон коуча/импорта не делался — кэш и грипперы подтвердятся первым реальным использованием.

### ✅ Сделано (`5b224df`)
- **Prompt caching (anthropic)**: ephemeral `cache_control` на system + последнем туле + последнем блоке последнего сообщения (макс. 3 брейкпоинта из 4). В агент-цикле коуча каждый следующий вызов читает весь префикс из кэша (×0.1 к цене инпута). `input.messages` НЕ мутируются (коуч переиспользует массив между турнами — иначе брейкпоинты копились бы). Учёт честный: адаптер прокидывает `cache_creation/cache_read`, гейтвей считает кост с множителями 1.25/0.1, а токен-капы видят полный размер промпта.
- **Таймауты**: `AbortSignal.timeout(120s)` во всех трёх адаптерах → `AiError('provider_timeout')` (раньше зависший провайдер держал функцию до её жёсткого лимита).
- **Миграция `20260704090000`**: `ai_usage.intent` (в PK; расход теперь виден в разрезе coach_chat/program_import/transcribe, старые строки → `intent=''`); `ai_limits.user_month_cost_cap=$20` — разогнавшийся юзер блокирует себя, а не весь ИИ обоим (урок инцидента $89M); `ai_budget_check`: день агрегируется sum'ом по интентам, user-кап проверяется до глобального. `ai_record_usage` пересоздана с `p_intent default ''` — вызов `transcribe` без параметра совместим (и обновлён на `p_intent:'transcribe'`).
- **Порт грипперов дня-43 в `program-import`**: структурные `gripper`/`rgc`/`set_type` в JSON-схеме и промпте (вкл. алиасы діп-сета и «RGC → число, не в weight/notes»); копия резолвера workout-import (двунаправленный subset токенов + кириллич. гомоглифы + ближайший RGC, личные первыми); `meta.gripper_id`/фолбэк `gripper_model`+`gripper_rgc`/`set_type` пишутся в `program_sets.meta` — `buildWorkoutFromProgram` уже копирует meta в сеты тренировки как есть; гриппер-упражнение принудительно каноническое «Стиснення еспандера» (`log_kind='gripper'`); вес грип-сета всегда null.
- **Гигиена**: `scripts/tmp-smoke-notify.mjs` → закоммичен как постоянный `scripts/smoke-notify.mjs` (`120f070`).
- Деплой: `db push` + `functions deploy coach-chat|workout-import|program-import|transcribe`.

### ✅ Аналитика → SQL RPC (`b37eb74`, продолжение той же сессии)
- **Миграция `20260704100000`**: `get_analytics_summary()` (security definer, `auth.uid()`) — агрегаты по тренировкам (тоннаж/сеты/RPE/время), топ-5 рекордов вес×повторы и время на упражнение, топ-3 грип-рекордов на установку; вся семантика 1-в-1 со старым клиентским `analyze()`. **Сверка на живых данных Сергея** (юзер-сессия через magiclink + реплика старой агрегации): 16/16 тренировок, 130 сетов, тоннаж 40055.95 до копейки, все топы совпали; ответ RPC 12КБ. + индексы аудита: `workout_exercises(exercise_id)`, partial `sets(logged_at)`.
- **Клиент**: `analytics.ts` → RPC; экран потребляет готовые агрегаты (запросы каталога грипперов и веса тела с экрана убраны — всё в SQL). Персист кэша в AsyncStorage больше не растёт с историей (лимит ~6МБ был реальным риском).
- **Инвалидация (находка `/code-review`)**: `['analytics']` в exercises/index.tsx был мёртвым всегда (не матчил `['analytics-sets']`); ключ сводки теперь `['analytics','summary',userId]`, а финиш/удаление/импорт тренировки инвалидируют `['analytics']` префиксом.
- Ревью: `/code-review medium` (6 углов параллельно) — подтверждена только цепочка инвалидации (починена), остальные кандидаты опровергнуты построчно; `tsc` ✅; `eas update` группа `4422bb86`.

### ⏭️ Следующее
- На устройстве: таб «Аналітика» после апдейта — числа/рекорды/календарь без изменений. Эффект кэша коуча — по `ai_usage.intent`. Грипперы в импорте программы — живым текстом. Остальные кандидаты — в START HERE.

---

## 2026-07-03 (день-47) — APK живёт, EAS этап 2 (remote push) закрыт, тёмные диалоги, хвосты лидерборда

APK `9b1f330a` установлен на S25 (старую Expo-сборку Сергей снёс) — весь день итерации по живому устройству через `eas update`. Верификация: `tsc` после каждого блока, миграции по чек-листу, `ai-gateway-reviewer` на правку `workout-import`, пуши проверены на устройстве во всех трёх состояниях апки (foreground/свёрнута/убита) + receipt-проверкой Expo Push API с компа.

### ✅ Сделано
- **Нотификации не оставались в шторке** (`64b3d5b`): канал `leaderboard` создавался, но не использовался — `trigger: null` без `channelId` уводил в фолбэк-канал expo. Теперь канал `leaderboard-v2` (HIGH, heads-up), старый удаляется, `channelId` в trigger.
- **Три бага одной раскопкой** (`c5fc579`): (1) ошибка PostgREST — простой объект, НЕ `instanceof Error` (postgrest-js без throwOnError, `PostgrestBuilder.ts:542`) → маппинг `daily_entry_limit` не срабатывал и любая серверная ошибка подачи показывалась как «перевір посилання»; (2) догоняющая сверка статусов работала только на холодном старте → теперь и на возврате в foreground (AppState; Android морозит JS в фоне, realtime-события теряются без реплея) + инвалидация кэшей борда; (3) миграция `20260703070000` — антиспам 5/24ч не действует на admin (Сергей упёрся в лимит своими же тестами).
- **EAS этап 2 — remote push** (`7ef836b`, BACKLOG §9): миграция `20260703090000` `push_tokens` (RLS own-rows, PK user_id+token — общий телефон двух акков ок); `registerPushToken` при старте (Expo Go молча скипается); edge `push-entry-review` (деплой `--no-verify-jwt`, авторизация внутри: service-ключ для смоук-скриптов или JWT с ролью admin; статус из БД — спуфить нельзя; локализация по `profile.language`; чистка DeviceNotRegistered-токенов) + fire-and-forget invoke из `reviewEntry`. Дедуп слоёв: remote в foreground глушится хендлером (покажет realtime), sweep не дублирует пуш, уже висящий в шторке (ключ `entryId:status` в data).
- **FCM-ключ**: первый пуш умер с `InvalidCredentials` в тикете при HTTP 200 — в EAS не был загружен FCM V1 service-account ключ (этап 1 дал только `google-services.json` = приёмник). Сергей сгенерил ключ в Firebase console и загрузил на expo.dev/credentials. Урок → фикс функции (`abfdd4c`): `sent` считается только по тикетам `status: ok`, ошибочные тикеты в лог и в ответ.
- **Тёмные диалоги** (`af155eb`): `useAppDialog` (обёртка `ConfirmDialog`, `cancelLabel` теперь опционален → одна кнопка = алерт) — все 5 системных `Alert.alert` заменены (ошибка/лимит подачи, удаление заявки, удаление эспандера, кап кастомных упражнений ×2). В апке не осталось нативных алертов.
- **Хвосты лидерборда** (`af155eb`): Акаунт — свитч цикла скрыт для grip; экран `/proof-rules` (6 правил видео-пруфа en+uk, ссылка из формы подачи); поле «Дата результату» дд.мм.рррр (валидация: реальная дата, не будущее) → `performed_at` (борд уже умел показывать). Нативный датапикер сознательно не ставился (пересборка APK — батчить с блюр-таббаром).
- **Терминология + правила** (`8f43b2b`, `c2eb72f`): «Дип-сет» → «Діп-сет» по всей укр. локали; правило 3 — любое закрытие с установкой второй рукой (кроме карты) = діп-сет независимо от зазора (5 или 35 мм); правило 5 — Certified только с официальным видео успешной попытки IronMind. `workout-import`: алиасы «діпсет»/«діп-сет» в хинте set_type (ревьюер ок, задеплоен).
- **Прод-гигиена**: мок-лидерборд снесён (45 юзеров, каскад); все тестовые заявки смоуков удалены; `eas update` канал preview подтверждён как рабочий путь доставки.

### ⏭️ Следующее
- **Apple/TestFlight (этап 3 §9) — только по явной команде Сергея.** Остальные кандидаты — в START HERE.

---

## 2026-07-02 (день-46, вечер) — итерации 4–5 лидерборда, realtime-нотификации, EAS этап 1 (FCM + preview-билд)

Продолжение дня-46: две итерации по фидбеку Сергея с устройства, затем — по решению §9 — старт ухода с Expo Go: FCM-конфиг и первый preview-билд APK. Верификация: `tsc` ✅, веб-смоук живым акком Сергея (поиск «coc 3» во всех пикерах, лейблы, дата по тапу на борде, календарь→summary), обе миграции накатаны `db push`. Нотификации на устройстве не проверялись (web их скипает) — первым делом после установки APK.

### ✅ Сделано
- **Итерация 4** (`2408032`): единый нормализованный поиск эспандеров `normSearch`/`gripperMatches` (`src/lib/db/grippers.ts`) во **всех трёх** пикерах (заявка на борд / «Мої еспандери» / тренировка) — «coc 3» находит и CoC #3, и #3.5; «Власна вага» вместо «ВВ»; префикс «RGC:» в лейблах пикеров и строках борда; серт-плашки «CoC 3 Certified» на **обоих** бордах (`certLabels` считается от gripper-борда, второй query с тем же ключом кэша). **Realtime-нотификации о судьбе заявки**: `src/lib/entry-notifications.tsx` — два слоя: postgres_changes на свои заявки при живой апке (RLS-фильтр) + догон на старте сверкой с AsyncStorage-снимком (первый запуск сеет молча); тап по нотификации → борд; web скипается (динамический импорт `expo-notifications`); канал Android + пермишены запрашиваются в осмысленные моменты. Dep `expo-notifications` установлен; миграция `20260702170000` (таблица в publication `supabase_realtime`). Remote push в Expo Go невозможен (вырезан с SDK 53) — это и породило решение §9.
- **Итерация 5** (`9fc5595`): тап по строке борда раскрывает «Дата результату» (`performed_at` → фолбэк `created_at`; RPC `get_leaderboard` пересоздан с `created_at`, миграция `20260702180000`); грип-рекорды аналитики приведены к шаблону обычных (позиция × повторы слева, ≈оценка · дата справа, «RGC:» подстрочником, внутренняя карточка bg-800); «40.1к» с локализованным суффиксом тысяч, «7 год 5 хв» с пробелами, все даты дд.мм.гг с ведущими нулями (аналитика, столбцы тоннажа, программы — там был US-формат); календарь: «Липень 2026» без капслочного «Р.» + из модалки дня кнопка «Відкрити тренування» → `/summary/[id]` (в `DayInfo` добавлены `workoutIds`).
- **Решение о дистрибуции** (`79612dd`): BACKLOG §9 — уход с Expo Go на EAS-билды; драйверы: пуши, iPhone Маши, Google Play, снятие заложничества пина SDK 54. Этапы 1–5 расписаны там же.
- **EAS этап 1** (`7b6e630`): Firebase-проект `sporty-sm` (только FCM, БД не его), `google-services.json` в корне репо (репо приватный — коммитим), `app.json`: `googleServicesFile` + плагин `expo-notifications` (цвет #1FB89A). Keystore у EAS уже был (с дня-44) → билд ушёл без интерактива: `9b1f330a-1832-4c12-a6e0-0667ac9c0e78`, профиль `preview` (APK, канал `preview`). На конец сессии — IN_QUEUE >1.5ч (вечерний пик фри-тира; вывод: билды лучше катать утром).

### 🚧 TODO (деплой/утро)
- Билд `9b1f330a`: дождаться → APK на S25 (рядом с Expo Go) → логин → смоук: данные на месте, борд, реалтайм-нотификация (апрув тестовой заявки из `/moderation` при свёрнутой апке → шторка).
- `eas update --channel preview` — канал JS-обновлений для APK (уточнить, что прежние `--branch preview` публикации совместимы — ветка и канал preview связаны в eas.json).
- Мок в проде → `node scripts/leaderboard-mock.mjs clean` (нужен `SUPABASE_SERVICE_ROLE_KEY` из `.env`).

### ⏭️ Следующее
- **EAS этап 2 — push-инфра** (BACKLOG §9): таблица `push_tokens` (RLS) + `getExpoPushTokenAsync` на клиенте + отправка из Edge Function после `review_leaderboard_entry` (Expo Push API). Realtime-слой остаётся как мгновенный канал внутри живой апки.
- Хвосты лидерборда: Акаунт для grip (урезать OURA/цикл), экран правил видео-пруфа, поле «дата выступления» в заявке.
- **Накопленные хвосты (перенесено из старого START HERE при компактизации):** блюр-таббар `expo-blur` (натив — батчить со следующей пересборкой APK, теперь это реально: этап 2/3 §9); порт фикса грипперов дня-43 в `program-import` (+ прогон `ai-gateway-reviewer`); серверные хвосты аудита дня-46: prompt caching в anthropic-адаптере (~-85% инпут-коста коуча), per-user месячный кап + колонка `intent` в `ai_usage`, таймауты (`AbortSignal.timeout`) в адаптерах, `personal_records` реализовать-или-выкинуть, аналитика→RPC (уйти от full-history на клиенте), сессия→SecureStore, RPC удаления аккаунта (требование Play Store — актуально к этапу 4 §9), `deno check` перед деплоем функций; коуч: явные треды/«Нова розмова» + дата-разделители, тон (день-31); премиум-лимит записи голосового >20с (`MAX_RECORDING_SEC`); навбар edge-to-edge проверить на устройстве (день-44 хвост C); аватарки в `assets/avatars/` (день-41).

---

## 2026-07-02 (день-46) — аудит → курс на грип-комьюнити: роли + гейт ИИ + схема лидербордов

Сессия из двух частей. Сначала **жёсткий аудит всего проекта** (архитектура/БД/ИИ/безопасность/перф/продукт) — по его итогам Сергей зафиксировал курс: **не массовый продукт, а «мы двое (full) + грип-комьюнити Украины (grip)»** — комьюнити получает только трекинг грипперов и лидерборды, без ИИ (бюджет $30 остаётся на двоих). Решения по лидербордам: два борда — динамометры (катируются GM-150 любой версии, XF-300, GD; канонично кг) и эспандеры (катируются закрытия TNS/карта/дип-сет); верификация — обязательный видео-URL (YouTube и т.п., видео НЕ храним) + ручной апрув админа. Верификация кода: `tsc` ✅, `ai-gateway-reviewer` ✅ (блокеров нет), миграции по чек-листу, роли/сид/RPC проверены read-only-скриптом на живой БД. Коуч после гейта живьём не дёргался (Сергей проверит с телефона). Тестов нет.

### ✅ Фиксы аудита (сразу закрытые)
- **Оффлайн-таймстемпы** (`d5c159a`): `finishWorkout`/`setSetLogged`/`setExerciseDone`/`addSet` получают время ТАПА через vars мутации — раньше queued-мутация писала на сервер время реконнекта (`ended_at` утром → «тренировка 14 часов», `completed_at` подходов = момент синка → перемешанный порядок). Все call-sites `workout/[id].tsx` обновлены.
- **Гигиена ИИ** (`99485a3`): лимит сообщения коуча 2000 симв. (вброс уходил в модель целиком и навсегда оседал в `ai_messages`, удорожая историю); санитизация `query` в `get_exercise_history` (интерполяция в PostgREST `.or()` на service-role — как уже было в workout-import); openai/gemini-адаптеры молча игнорили `tools` → теперь `AiError('tools_unsupported')` (смена роута coach_chat больше не ломает коуча тихо).

### ✅ Роли доступа + серверный гейт ИИ (`08e3317`, миграция `20260702120000`)
- `user_roles`: `grip` (дефолт новорегов через `handle_new_user`) / `full` / `admin`. Отдельная таблица, НЕ колонка в profile (юзер правит свою строку профиля — выдал бы себе full сам). RLS select-own, клиентских write-политик нет. Бэкфилл: существующие → full, Сергей → admin. Правило задокументировано в AGENTS.md.
- Все 4 ИИ-функции (`coach-chat`, `workout-import`, `program-import`, `transcribe`) проверяют роль через `_shared/roles.ts` **до любых платных операций** → grip-юзер не дёрнет ИИ даже curl'ом мимо спрятанных табов. Ревьюер подтвердил порядок гейтов и отсутствие утечек ключей.

### ✅ Схема лидербордов (миграция `20260702130000`)
- `dynamometers` — справочник катируемых (GM-150/XF-300/GD), новая модель = строка, не миграция.
- `leaderboard_entries` — либо динамометр+`weight_kg`, либо `gripper_id`+`set_type` (tns/card/deep), shape-constraint; `video_url` обязателен (https, ≤300); `status` pending→approved/rejected. RLS: свои видны всегда, admin видит всё, insert только своя+pending, update только пока pending, delete своей — всегда. Анти-спам: триггер 5 заявок/24ч.
- `get_leaderboard(board)` — security definer витрина: ТОЛЬКО approved и только имя/аватар/железка/результат (email не выходит). `review_leaderboard_entry` — модерация, только admin. Оба revoked от anon.

### 📋 Аудит: что зафиксировано в бэклог (не делалось)
Prompt caching в anthropic-адаптере; per-user месячный кап + `intent` в `ai_usage`; таймауты в адаптерах; `personal_records` мёртв (никто не пишет — реализовать или выкинуть тул коуча); аналитика качает всю историю сетов на клиент и персистит в AsyncStorage (лимит ~6МБ Android) → RPC; сессия в plaintext AsyncStorage → SecureStore; RPC удаления аккаунта (требование Play Store); `supabase/functions` вне tsc и deno нигде не стоит → `deno check` перед деплоем; индексы `workout_exercises(exercise_id)` + partial `sets(logged_at)`.

### ✅ Грип-режим UI + экран лидерборда (вторая часть сессии, `2291253`)
- `src/lib/use-role.ts` (`useRole`, кэш персистится) → таб-бар по роли: grip видит **Тренування/Аналітика/Борд** (`href:null` прячет programs/coach/health), ИИ-импорт на главной скрыт для grip (сервер и так 403).
- Таб **«Лідерборд»** (`(tabs)/leaderboard.tsx` + `src/lib/db/leaderboard.ts`): сегмент динамометри/еспандери, чипы-фильтры (прилад / tns-card-deep), лучший результат на юзера (`bestPerUser`), медали топ-3, видео-пруф по тапу (`Linking`); подача заявки в BottomSheet (динамометр в единице юзера→кг канонично; эспандер из каталога `listGripperCatalog` + сет-тип; https-валидация URL; человеческая ошибка на лимит 5/сутки); «Мої заявки» со статусом/отзывом; **модерация admin инлайн** (апрув/реджект). i18n en+uk в паритете.
- RPC `get_leaderboard_pending` (миграция `20260702140000`) — админ-очередь с именами через security definer (profile чужих клиенту не виден).
- **Верификация:** tsc ✅; веб-смоук живым grip-аккаунтом: регистрация → триггер выдал `grip` → 3 таба → заявка GM-150 62.5 кг → «На перевірці», на борде скрыта ✅; DB-смоук: апрув делает видимой с именем/приладом, shape-constraint отбивает кривую форму ✅; тестовый юзер удалён каскадом (entries=0).

### ✅ Итерация 2 по скринам с устройства (`b553301`, третья часть сессии)
Фидбек Сергея: «Athlete» без возможности задать ник; модерация мешается на борде; сертифицированное закрытие ≠ домашнее с карты; в пикере эспандеров личные замешаны с каталожными; ссылки открываются без предупреждения (фишинг-риск).
- **Никнейм**: поле в шапке Акаунта (`display_name`, blur+`onEndEditing` — урок дня-45), подпись «видно на лідерборді».
- **Админ-панель `/moderation`** (stack-роут как account): карточки заявок с «Дивитися пруф»/апрув/реджект; на борде у админа осталась только кнопка со счётчиком pending.
- **Сертификация**: колонка `certified` (миграция `20260702150000`) + свитч в заявке + бейдж-лента на борде/в «Мої заявки»/в модерации + приоритет над несертифицированным при равном кг/RGC (`bestPerUser`).
- **Ссылки-пруфы**: check-constraint БД + клиентская валидация — только YouTube/youtu.be/Instagram/TikTok (vm./vt.)/Vimeo/Facebook; открытие любой ссылки — через `useConfirmedVideoLink` (диалог: домен + полный URL). Первый `db push` упал об реальную vt.tiktok-заявку Сергея → regex расширен, данные целы.
- Пикер эспандеров: секции «Мої (заміряні)» / «Каталог (середні)», поиск нормализован («coc 3» → «CoC #3»). Pull-to-refresh на борде. Таб-лейблы 10pt.
- **Мок**: `scripts/leaderboard-mock.mjs seed|clean` — 45 юзеров/97 заявок засеяны в прод (~80% approved, 15% pending для админки), борд на объёме проверен в смоуке (медали/инициалы/RGC-ранжир). Уборка одной командой.

### ✅ Итерация 3 (`54d1dbe`, по второй пачке фидбека)
- **Сертификация только там, где она существует**: свитч виден только при выборе CoC #3/#3.5/#4 (`certEligibleGripper`), динамометр — без серта; вместо зелёной ленты у ника курсивом «CoC 3 Certified» / «CoC 3, 3.5 Certified» (агрегация по всем approved-заявкам юзера, `certLabels`).
- **Власна вага** в строке борда («· ВВ 95 кг» серым) — миграция `20260702160000` добавила `bodyweight` в `get_leaderboard` (вес из профиля участника; участие = согласие на публичный результат — приватность обсуждена, ок для комьюнити).
- Подсветка рамкой — **своя позиция**, не первая (путала); медали топ-3 крупнее; галочка сохранения имени в Акаунте; таб-бар **только иконки** (подписи резались); uk-юнит «lbs» вместо «фунти».
- Таб «Здоров'я» — **дата-гейт**: виден только при `oura_connected`/`track_cycle` (Маша), не роль.
- Поиск «coc 3» → находит и #3, и #3.5 — уже закрыто в итерации 2 (нормализация).
- Верификация: веб-смоук на живом моке (серт-лейбл, ВВ, условный свитч Heavy Grips vs CoC, 3 иконки), tsc ✅, тестовый юзер удалён. Мок дополнен: ВВ у всех 45, у OdesaSqueeze серт CoC #3+#3.5.

### ✅ codebase-memory-mcp (инструментарий, не прод)
Установлен MCP-сервер [DeusData/codebase-memory-mcp](https://github.com/DeusData/codebase-memory-mcp) v0.8.1 (локальный Go-бинарник, `%LOCALAPPDATA%\Programs\`, без ключей/облака): AST-граф кодовой базы в SQLite (2040 узлов/3699 рёбер, индекс ~1с) + инструменты search_graph/trace_path/get_architecture. Сам сконфигурил Claude Code (`~/.claude/.mcp.json`, skill, PreToolUse-хук на Grep/Glob). **Активен со следующей сессии Claude Code.** На втором компе поставить: `Invoke-WebRequest …/install.ps1` + `codebase-memory-mcp install`. Переиндексация: `codebase-memory-mcp cli index_repository '{"repo_path": "…"}'` (сам детектит git-изменения).

### ⏭️ Следующее
- Сергей: `eas update` (итерации 2+3 на телефон), посмотреть мок → `node scripts/leaderboard-mock.mjs clean`; проверить коуча живьём после ролевого гейта.
- Хвосты: Акаунт для grip (спрятать OURA/цикл в настройках), пуш/бейдж о смене статуса заявки, экран правил видео-пруфа, дата выступления в заявке.

---

## 2026-07-01 (день-45) — пилар «Программы»: ручной конструктор + фиксы по фидбеку с устройства

Сессия по фидбеку Сергея с телефона (APK дня-44). Начали со сверки `buildWorkoutFromProgram` → нашли реальный баг: старт из старой ИИ-программы «Жим лежачи» (импорт 15.06, до переделки дня-30б) молча терял 2/3 упражнений (`exercise_id=null` + `NOT NULL` FK на `workout_exercises` → тихий `continue`). Данные НЕ трогали. Корень глубже: создание программ было **только** через ИИ, ручного пути в каталог не было вообще (`addProgramExercise` не существовал). Построили пилар. Верификация — `tsc`✅ после каждого шага, веб-смоук (сборка+загрузка) ✅. Всё в main (`147990c`), **НЕ задеплоено — только клиент**. Тестов нет.

### ✅ Ручной конструктор программ
- `createProgram` (+ кап `MAX_USER_PROGRAMS=3` — гигиена БД, 📦) + `addProgramExercise` из каталога.
- Общий `ExercisePicker` вынесен из `workout/[id]` в `src/components/exercise-picker.tsx` (переиспользование лог+конструктор). «+ своє» → `createCustomExercise` → добавляет в прогу.
- Блоки **EMOM/E2MOM/суперсет/AMRAP**: `createProgramBlock`/`deleteProgramBlock`, `BlockConfigModal` (тип+параметры), `groupProgram` теперь показывает пустые кластер-блоки. `programs.tsx`: «Створити» основная кнопка, ИИ-импорт вторичная, кап-гейт.

### ✅ Корректность подхода (фидбек с устройства)
- **Оптимистика** добавления/правки подходов (`onMutate` в кэш) — нет лага и мульти-тапа (раньше тап без отклика плодил лишние подходы).
- **Метрика:** `EditableSet` читает `metric` упражнения → вага/**секунди** для удержаний, не вага/повтори.
- **Сторона (side)** прошита ПО ВСЕЙ цепочке: `program_sets.meta` → `buildWorkoutFromProgram` переносит `meta` (был хардкод `null`) → `persistStartedWorkout` пишет `meta` → аналитика «обидві» ×2. Заодно чинит латентную потерю meta и у ИИ-программ.
- **Суперсет = явные подходы** (каждый = круг, пирамида 12/10/8): `repeat=1` без умножения; при добавлении в суперсет предсоздаём N подходов. Безопасно для ИИ-импорта (он разворачивает подходы явно; блоков в проде нет).

### ✅ Потеря последнего введённого значения (bug fix, program + workout)
- **Программа:** коммит подходов «на лету» (эффект при изменении weight/reps/secs, оптимистично) — не зависит от blur/фокуса.
- **Тренировка:** SetRow тоже коммитит вес/повторы «на лету» (эффект при изменении weight/amount, `13aa667`). Сначала пробовал `flushThen` (Keyboard.dismiss+rAF на завершениях) — НЕ сработало: dismiss не триггерит `onEndEditing` надёжно на устройстве. **Проверено в превью:** ввёл reps=12 → сразу «Finish exercise» без blur → reps=12 на сервере (DB-подтверждено). `flushThen` оставлен (безвредно дизмиссит клавиатуру).
- **Авто-отметка «зроблено» при вводе** (`f1a0044`, по видео Сергея): корень «завершив, а всё 0» — `workout_summaries` считает ТОЛЬКО подходы с `logged_at` (отмеченные ✓). Юзер вводил значения, но ✓ не жал → всё обнулялось (данные в БД есть, но не в зачёте; подтверждено данными — 3 тренировки, все `logged=null`). Решение (выбор Сергея, «варик 1»): ввод повторов/сек авто-помечает подход сделанным (`markSetLogged`, restSec=null). Нюанс осознан: программный предзаполненный подход тоже зачтётся — лучше увидеть+удалить, чем тапать ✓. **Проверено превью+БД:** 30×15 без ✓ → «Завершити» → сводка 1/1/15/450.

### ✅ ПР взвешенных удержаний — по весу
- Щипковый хват и т.п. ранжируются по МАКС ВЕСУ, не по секундам (`analytics.tsx`: сортировка `(b.weight??0)-(a.weight??0)||b.sec-a.sec`, новый вид заголовка `timeWeight`). Планка/вис (телесные) — по времени, как было. Секунды пишутся и показываются.

### ⏭️ Следующее
- **Мелкий хвост:** в `BlockConfigModal` лейбл суперсета «Раунди» теперь = число кругов/подходов (не множитель) — при желании уточнить формулировку.
- **`personal_records` в `coach-chat`:** если коуч озвучивает рекорды оттуда — возможен старый ранжир удержаний (отдельный трек от аналитики, что починили).
- Прежний план: **треды коуча** (§3, «одно полотно» → сессии + threadId + история, заодно дешевле/точнее контекст); тон коуча (хвост дня-31).

---

## 2026-07-01 (день-44) — автономный APK + EAS Update (OTA) + вес тела в тоннаже + UI-полиш по устройству

Большая сессия «в поле»: Сергей поставил APK на S25 и гонял вживую, фидбек по скринам. Верификация — `tsc` ✅ после каждого шага (тестов нет); грипперы/вес тела подтверждены визуально в проде; правок ИИ-гейтвея не было (`ai-gateway-reviewer` не требовался). Всё в `main`, доставка — часть пересборкой APK, часть по OTA.

### ✅ Доставка (EAS)
- **Автономный APK**: `eas login` (аккаунт `xzsergeyzx`) + `build:configure` (projectId `eaccb3d3`, привязка) → `eas build -p android --profile preview` → `.apk` на S25, работает **без Metro**. Бэкенд-ключи зашиты (день-36) → коннектится сам.
- **EAS Update (OTA)**: `update:configure` → `expo-updates`, `updates.url`, `runtimeVersion={policy:appVersion}`, каналы dev/preview/prod. **JS-правки → `eas update --branch preview` без пересборки** (первая OTA уже опубликована — сводка/грипперы/таб-бар). Нативка (новые модули/permissions/иконка) → пересборка. Память [[eas-update-flow]] — напоминать про дрейф версий между 2 компами+телефоном; индикатор версии/OTA-id внизу Акаунта.

### ✅ Вес тела в тоннаже (SPEC §3.7)
- Миграция `20260630120000_bodyweight_load` (аддитивная, в проде): колонка `exercises.bodyweight_load` + флаг «честным» весо-телесным (подтягивания вкл. с резинкой/негативные/одноручные по паттерну, брусья/кольца Ring Dip, пистолет, канат; отжимания/пресс/планка — НЕ флагуются); пересборка вью `workout_summaries` (тоннаж = эффективная нагрузка `доп.вес + вес_тела`).
- Клиент: `profile.bodyweight` ввод в Акаунте (кг канонически); `workoutStats(bw)`, `analyze/analyzeCorrelation/dayInfo` берут `bwKg`; строки сводки тоже показывают эффективный вес (подтягивания → «95 кг × 15», не «– кг»). Сергей вписал 95 → тоннаж 1805 сходится.

### ✅ Грипперы (день-43 задеплоен + полиш)
- `functions deploy workout-import` (резолвер дня-43) — **подтверждён Сергеем в проде**: личный каталог, RGC в кг, L/R, кириллица.
- UI: kg-первично + lb в скобках (`gripperLabel(g, unit)`, вкл. lb-замеренный Standard W → кг); сортировка «Мої еспандери» по RGC(кг)↑ → имя; каталог подставляет среднее в кг (`rgcInUnit`); повторы грипперов в сводке «8 повторів» (было «× 8»); `BottomSheet` KeyboardAvoidingView.

### ✅ UI-полиш по фидбеку с устройства
- Здоров'я: инлайн-описания/нормы на карточках вместо «ТАПНИ, ЩОБ ПРОЧИТАТИ» ×10 (тап всё ещё открывает полный лист).
- Главная: единый ScrollView (шапка скроллится — нет «обрезанной» карточки); корзина убрана с карточек → удаление внутри сводки; фикс kg↔lb недельного тоннажа; эмодзи `🗑`/`▸` → Ionicons.
- Низ под Android: `userInterfaceStyle:dark` + `androidNavigationBar` (тёмный, цвет таб-бара) + нижний индент таб-бара (иконки не липнут к системному бару).
- Фикс мигающего ввода веса (focus-aware controlled, без null-трюка).

### ⏭️ Следующее (см. START HERE): (A) блюр-таббар — забатчить с нативными (пересборка); (B) порт фикса грипперов в `program-import`; (C) проверить навбар на устройстве.

---

## 2026-06-29 (день-43) — фикс грипперов в импорте тренировки (RGC + личный каталог + L/R)

Сергей (удалённо с телефона) пожаловался на кривой импорт последней тренировки. Залез в прод read-only-скриптами, разобрал по факту. Импорт **не теряет данные на ровном месте** — просто резолвер грипперов был сломан. Чиню фикс-форвардом в импорте (правило проекта: не латать данные ретро-слиянием — однажды уже потеряли). `tsc` ✅; новый резолвер прогнан изолированно на реальном каталоге Сергея — **6/6**; `ai-gateway-reviewer` запущен. **НЕ задеплоено** — Сергей завтра с компа перезальёт, проверит, тогда `functions deploy`.

### 🔎 Что было сломано (тренировка `9cb32be9`, 27.06)
- **Грипперы.** Резолвер матчил «имя_каталога ⊆ текст», а личные имена Сергея *длиннее* вводимого («Heavy Grips 200 (Temu)» vs он пишет «Heavy Grips 200») → личный не матчился, цеплялся глобальный из чарта (88 lb вместо его 47 кг). «СоС 3» (кириллица) ≠ «CoC #3» → дроп (потеря гриппера). RGC из текста выкидывался в notes → две CoC#2.5 (56/59) и три HG300 (74/72/72) не различались.
- **Одноручные подтягивания.** «два подхода по 1 разу на каждую руку» → ИИ выдал `side=both` вместо right/left (объём считался ×2).
- **Тоннаж.** Подтягивания лежат `weight=null` → 0 в тоннаж (фичи «вес тела» в приложении нет — это by design; жалоба на «95 кг как вес тела» **не воспроизвелась**: `profile.bodyweight=null`, числа 95 в коде нет — ждём от Сергея, где он это видит).

### ✅ Сделано (`supabase/functions/workout-import/index.ts`)
- **Резолвер `resolveGripperId(model, rgc)` переписан:** двунаправленный subset токенов (текст ⊆ имя И наоборот → «heavy grips 200» матчит «...200 (Temu)»), кириллич. гомоглифы (с→c, о→o…) → «СоС 3» = «CoC #3», и **различение одномодельных по ближайшему RGC** (личный важнее глобального при равенстве). Каталог теперь грузит `rgc, rgc_unit`.
- **Захват RGC из текста:** поле `rgc` добавлено в JSON-схему и `PSet`; правило промпта «RGC → число в поле `rgc`, не в weight/notes».
- **Fallback-мета:** если модель не сматчилась — пишем `meta.gripper_model`/`meta.gripper_rgc`, не теряем.
- **Одноручные:** правило промпта — single-arm / «на кожну руку» → всегда right+left, никогда `both`.
- **Никакого автосоздания грипперов и правки данных** — каталог Сергея уже полный и верный, нужно было лишь правильно матчить.

### 🚧 TODO
- **Сергею (с компа):** удалить 2 кривые тренировки-дубля от 27.06 (`9cb32be9`, `cc2e3298`) → перезалить программу → проверить грипперы/RGC/L/L → если ок, `npx supabase functions deploy workout-import`.
- **Параллельный долг:** `program-import` несёт копию резолвера грипперов (см. день-39) — перенести туда тот же фикс, когда подтвердится на workout-import.

---

## 2026-06-29 (день-42) — EAS-конфиг под автономный APK (без Metro)

Запрос Сергея: собрать установочный APK, который запускается на телефоне **без Metro/Expo Go**. Это отложенная EAS-сборка (хвост дня-40). Локально нет Java/Android SDK → путь = **облачный EAS Build** (собирает на серверах Expo, нужен только бесплатный аккаунт + `eas login`, на выходе ссылка на `.apk`). Настроены конфиги; сама сборка требует интерактивного входа — за Сергеем. Менялся только JSON-конфиг (`tsc` не гонял — кода не трогали). **Закоммичено+запушено в main.**

### ✅ Сделано
- `app.json` → `android.package: "com.sportysm.app"` (постоянный id приложения на устройстве; смена = переустановка с нуля). Выбран Сергеем.
- `eas.json` (новый) → профили: `development` (dev-client, apk), **`preview` (apk, `distribution: internal` — «скачал и поставил»)**, `production` (autoIncrement); `cli.appVersionSource: remote`.
- Сверено: автономный APK подключится к бэку без `.env` — Supabase URL+publishable-ключ зашиты в `src/lib/supabase.ts` (день-36); RLS защищает данные, ключ публичный.
- `eas-cli` резолвится через `npx eas-cli` (v20.4.0); пакет называется `eas-cli`, не `eas` (поэтому `npx eas` падало).

### 🚧 TODO (Сергею, интерактив — следующая сессия)
1. `npx eas-cli login` (нет аккаунта Expo → завести на expo.dev, бесплатно).
2. `npx eas-cli build:configure` — создаст EAS-проект, допишет `extra.eas.projectId` в `app.json` → **закоммитить этот diff**.
3. `npx eas-cli build -p android --profile preview` — облачная сборка (~10–20 мин с очередью); keystore → дать EAS сгенерить самому. На выходе ссылка на `.apk` → открыть на S25 Ultra → установить (разрешить установку из неизвестного источника).

### ⏭️ Следующее
- После рабочего APK — вернуться к плану: (0) сверка `buildWorkoutFromProgram`; (1) треды коуча; (2) тон коуча (хвост дня-31).

---

## 2026-06-29 (день-40) — навигация: 6→5 табов («Акаунт» → шестерёнка), эмодзи → Ionicons

Запрос Сергея: привести нав к золотому стандарту (3–5 табов с иконками), не наделав катакомб; договорились начать с бесспорного — вынос 6-го таба «Акаунт» в шестерёнку + замена эмодзи. Анализ показал 2 реальных дефекта: 6 табов (подписи резались) и эмодзи-иконки (игнорировали active/inactive tint, разнобой с Feather в коуче). Верификация: `tsc --noEmit` ✅; web-бандл собрался и загрузился без ошибок (таб-бар рисуется после логина — визуал на устройстве). **Закоммичено+запушено в main (`7f6ae31`).**

### ✅ Сделано
- **«Акаунт» вынесен из таб-бара** в stack-роут `src/app/account.tsx` (был `(tabs)/account.tsx`). Импорты на `@/`-алиасах → не менялись. Добавлены back-кнопка (`‹`, паттерн как в `exercises`) и self-guard сессии (`Redirect href="/auth"`, т.к. экран теперь вне гейта `(tabs)`).
- **`SettingsButton`** (`src/components/settings-button.tsx`) — шестерёнка `Ionicons settings-outline` → `router.push('/account')`, в правом верхнем углу 5 экранов (workouts/programs/analytics/health — обёртка заголовка во `flex-row justify-between`; coach — в его bordered-шапке). accessibilityLabel = `tabs.account` (ключ переиспользован).
- **Иконки табов: эмодзи → Ionicons outline** (`_layout.tsx`): barbell / clipboard / chatbubble-ellipses / stats-chart / heart. Хелпер `icon()` теперь рендерит `<Ionicons color size>` → красится `tabBarActiveTintColor`/`Inactive`. Новой зависимости нет.
- **Правки по фидбеку с телефона** (`ef23b9e`): (#1) контент аккаунта лез под системную навигацию Android → `'bottom'` в `SafeAreaView` (account + exercises + grippers — все pushed-экраны без таб-бара). (#2) горизонтальный «недосвайп» при возврате (весь таб-навигатор с баром ехал вбок, т.к. account — root-stack поверх `(tabs)`) → account объявлен в рут-стеке с `animation:'slide_from_bottom'` + `gestureEnabled:false` (поднимается над таб-UI, назад — «‹»/системная). (#3) заголовок коуча был `text-xl` ≠ остальные → выровнен на `text-2xl`. Verified на web: 5 табов с Ionicons + active-tint, шестерёнка в коуче, заголовок коуча 24px. #1/#2 — Android-специфичны (проверка на устройстве). **Хвост:** в Expo Go системный predictive-back может всё равно показывать «peek» — это снимется только в реальном билде (`app.json android.predictiveBackGestureEnabled:false`), т.е. на EAS.
- **Кнопка «Вийти» → подтверждение + destructive** (`c2ed9e5`): снизу pushed-экрана аккаунта она читалась как «назад/готово», а тапом разлогинивала. Перекрашена в red-900 border + red-400 текст (не выглядит как навигация) и тап открывает `ConfirmDialog` (выход только после подтверждения). Ключи `account.signOutConfirm*` в en+uk.
- **Редизайн экрана аккаунта** (`edeef6d`, по «прожарке»): корень «уёбищности» — на экране дрались ДВА визуальных языка (Segmented selected был БЕЛЫЙ, чипы — бирюзовые). Унифицировал «выбрано» = accent (правка в `segmented.tsx` → бирюза; затрагивает и онбординг). Сгруппировал в iOS-style карточки-секции (Налаштування / Профіль / Каталог), «Мої вправи»/«Мої еспандери» стали нав-строками с Ionicons-иконками + шевроном + разделителями (а не голый текст с далёким ›), добавил блок идентичности (кружок-аватар с инициалами из email + email). Ключи `account.section*` в en+uk. Verified на web.
- **Убрана секция «Вправи в каталозі» с аккаунта** (`d07a908`): это фильтр каталога в пикере, а не настройка профиля. Функциональность цела — пикер (`workout/[id]`) сам включает дисциплину при добавлении упражнения из неё (`disciplineToEnable`→`enableDiscipline`), поиск фильтр игнорит. Если каталог разрастётся — управление добавить в сам пикер. «Каталог» теперь = только нав-строки «Мої вправи»/«Мої еспандери». (Ключи `account.disciplines*` оставлены на будущее.)
- **Гейтинг каталога по дисциплинам ВЫКЛЮЧЕН** (`aefdae4`): всплыло, что у нового юзера (`disciplines=[]`) спец-вправи (натяжка/армрестлінг) не видны в обзоре пикера — только через угаданный поиск (`isVisible` их прятал). Каталог пока крошечный (армрестлінг + сила хвата + база) → флаг `CATALOG_DISCIPLINE_GATING=false` в `exercises.ts`, `isVisible` пускает весь глобальный каталог всем. Машинерия дисциплин цела (auto-enable в пикере, ключи) — вернуть гейтинг = флаг в `true`, когда каталог разрастётся, и тогда же сделать вход в онбординге/пикере (НЕ на профиле). Это и закрывает «как Петро добавит натяжку».
- **Выбор аватарки** (`32523ea`, модель «картинки-пресеты»): аватар в идентити-блоке тапабелен (карандаш-бейдж) → bottom-sheet пикер (дефолт-інініали + сетка пресетов, выбранное в accent-обводке). Хранение — `profile.avatar` (ключ), миграция `20260629120000` применена в прод. Картинки бандлятся из `assets/avatars/`, реестр `src/lib/avatars.ts` (статический require — по строке на файл). **Добавлены 8 пресетов** (boy/girl/foxM/foxF/cat/dog/panda/gorilla), ужаты до 256×256 через jimp (~9МБ→898КБ; исходники были ~1МБ — для кружка 48-64px избыточно). На web проверено end-to-end: пикер показывает все 8, выбор отрисовывается, переживает релоад (persist в `profile.avatar`), возврат к инициалам работает. Кастом-загрузку фото осознанно НЕ делали (📦/💰-хотспот). Компонент `Avatar` (картинка|инициалы), `profile.getAvatar/setAvatar`, ключи `account.avatar*`.

### ⏭️ Следующее
- **Сергею:** `git pull --ff-only` → проверить таб-бар на телефоне (5 табов, иконки красятся, шестерёнка ведёт в аккаунт, «назад» работает).
- Навигация — обсудить дальше (НЕ решено): свернуть ли «Програми» в «Тренування» сегментом (→ 4 таба); слияние «Здоров'я» в «Аналітику» — **анти-паттерн, не делать** (разный ритм использования). EAS-сборка APK — отложена на отдельную сессию (нет `eas.json`, нет `android.package`, нужен вход в Expo-аккаунт).
- Прежний план: (1) треды коуча; (2) тон коуча (хвост дня-31); (0) ручная сверка `buildWorkoutFromProgram`.

---

## 2026-06-29 (день-39) — фаззи-матчинг импорта (namesResemble) + скрипт удаления имитации

Запрос Сергея: «матчинг крути, сделай красиво; имитацию минусануть (у меня её нет, у Маши мок); программы Маши удалю сам». Сделал фикс матчинга из плана дня-38 (п.а) + безопасный скрипт под удаление имитации. Верификация: `tsc --noEmit` ✅; namesResemble прогнан Node-скриптом на 14 кейсах (14/14); `ai-gateway-reviewer` — зелёный (путь ИИ/бюджет/ключи не тронуты). Тестов нет. **Закоммичено+запушено в main (`f4e6b8b`), обе функции задеплоены.**

### ✅ Сделано
- **`namesResemble` переписан** в `program-import/index.ts` и `workout-import/index.ts` (идентичные копии). Это предохранитель ПОСЛЕ смыслового матча ИИ — не матчинг сам по себе. Было: токены ≥4 букв по общему началу → отклоняло опечатки/слитное/короткие слова → дубль-кастомы. Стало: `normName` (ё→е, регистр, пунктуация), токены от 3 букв, `editDistance` (Левенштейн, O(n) память), `tokenMatch` (равно / длинное содержит короткое ≥4 / ~1 опечатка на 4 симв.), плюс проверка слитного написания (имена без пробелов). Кейсы: ✅ «жемлёжа»→«Жим лёжа», «жим», окончания, ё/е; ✅ отклоняет «млин з гирею»→«турецький підйом», «Ривок гирі», «Імітація руху».
- **`scripts/remove-imitation-exercise.mjs`** (новый) — удаление generic «Імітація руху» из каталога. Dry-run по умолчанию; удаляет ТОЛЬКО при нуле FK-ссылок (`workout_exercises`/`program_exercises`/`personal_records`), иначе печатает счётчики и пропускает. Читает service-role-ключ из env / `scripts/.service-role-key` / корневого `.env`.
- **«Імітація руху» (`af8a4435`) УДАЛЕНА из каталога.** Оказалось, программы её НЕ ссылались (`program_exercises=0`) — держала **одна незавершённая тестовая тренировка Маши** (`b1419acd`, 27.06, `ended_at=null`, не mock-seed; артефакт старта из кривой программы). С согласия Сергея тренировка удалена (каскадом WE+sets), затем имитация снесена скриптом. Каталог чист.

### ⏭️ Следующее
- **Сергею (по желанию):** удалить кривые программы Маши от 15.06 (мок, его действие) — на каталог уже не влияет, имитация снята независимо.
- Прежний план в силе: (0) ручная сверка `buildWorkoutFromProgram`; (1) треды коуча; (2) тон коуча (хвост дня-31). **STT-на-web снят с плана — веб не цель (Сергей на нём не работает).**

---

