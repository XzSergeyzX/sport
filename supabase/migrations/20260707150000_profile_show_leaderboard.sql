-- Тумблер «показувати Лідерборд»: комьюнити-борд (эспандеры/динамометр) нужен не всем —
-- выключенный тумблер прячет таб у конкретного юзера (фидбек: Маше он не нужен).
-- Это UI-преференция, не защита: RLS/видимость данных борда не меняются.
alter table public.profile
  add column if not exists show_leaderboard boolean not null default true;
