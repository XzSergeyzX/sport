# Скопируй этот файл в .env. Значения ниже — реальные публичные параметры проекта
# (Project Settings → API). publishable-ключ (sb_publishable_…) безопасен на клиенте,
# данные защищает RLS. Эти же значения зашиты как дефолт в src/lib/supabase.ts,
# так что без .env приложение тоже логинится.
# ВАЖНО: НЕ вставляй сюда старый legacy-ключ формата JWT (eyJ…) — он отключён Supabase.
EXPO_PUBLIC_SUPABASE_URL=https://sdvegejubjmmlnifvigt.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_-RWMi2w3VGgM_b_NjCxMVQ_LfKmD14i
